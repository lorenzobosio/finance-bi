---
phase: 00-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - next.config.ts
  - tsconfig.json
  - components.json
  - src/app/globals.css
  - src/lib/utils.ts
  - vitest.config.ts
  - test/allowlist.test.ts
  - test/middleware.test.ts
  - test/rls.assert.sql
  - .env.example
  - .gitignore
  - .planning/phases/00-foundation/PROVISIONING.md
autonomous: false
requirements: [FND-05, FND-06]
user_setup:
  - service: supabase
    why: "Postgres + Google Auth + RLS host; the project must exist before schema push (Plan 02) and auth (Plan 03)"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon/public key (or the sb_publishable_ key if the project issues new-style keys — see RESEARCH A3)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role key (or sb_secret_ key) — SERVER ONLY, never NEXT_PUBLIC_*"
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string -> Session pooler (port 5432) for migrations"
      - name: ALLOWED_EMAILS
        source: "Set by the developer: the 2 allowlisted Google emails, comma-separated"
    dashboard_config:
      - task: "Create the Supabase project (single prod project, no dev/prod split per D-18)"
        location: "Supabase Dashboard -> New Project"
      - task: "Enable the Google auth provider and paste the Google OAuth client id + secret (D-14: Google OAuth client configured in the Supabase dashboard)"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google"
  - service: google-cloud
    why: "Google OAuth client for Supabase Google sign-in (FND-01, consumed in Plan 03)"
    dashboard_config:
      - task: "Create an OAuth 2.0 Client (Web application) and add authorized redirect URIs: the Supabase callback URL (https://<project-ref>.supabase.co/auth/v1/callback), the Vercel production URL, and http://localhost:3000/auth/callback for local"
        location: "Google Cloud Console -> APIs & Services -> Credentials -> Create OAuth client ID"
  - service: vercel
    why: "Deploy target on Hobby tier linked to the GitHub repo (FND-05, deploy executed in Plan 04)"
    dashboard_config:
      - task: "Link the GitHub repo to a new Vercel project and set all 5 env vars (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, ALLOWED_EMAILS, DATABASE_URL) in Project Settings -> Environment Variables"
        location: "Vercel Dashboard -> Add New Project -> Import Git Repository"

must_haves:
  truths:
    - "The Next.js 15 app builds and runs locally via pnpm"
    - "A copied Tremor Raw chart can render (Recharts + cx util present) without the frozen @tremor/react package"
    - "Vitest runs and the Wave 0 test files exist and execute (red until later plans implement their targets)"
    - "Secrets are git-ignored; only .env.example (no real values) is committed"
    - "D-03: the application/package is scaffolded under the name finance-bi"
    - "D-14: the provisioning checklist configures the Google OAuth client in the Supabase dashboard (Authentication -> Providers -> Google)"
  artifacts:
    - path: "package.json"
      provides: "Next 15 + React 19 + Tailwind v4 + shadcn deps + Recharts/clsx/tailwind-merge/tailwind-variants + Vitest; NO @tremor/react"
      contains: "next"
    - path: "vitest.config.ts"
      provides: "Vitest test runner config"
    - path: "test/allowlist.test.ts"
      provides: "Failing unit test for isAllowed() (target created in Plan 03)"
    - path: "test/middleware.test.ts"
      provides: "Failing unit test for middleware redirect + non-allowlisted sign-out (target created in Plan 03)"
    - path: "test/rls.assert.sql"
      provides: "SQL assertions: RLS-on-every-table, zero-rows-for-non-allowlisted, seed counts (run against the live DB in Plan 02/04)"
    - path: "src/lib/utils.ts"
      provides: "cn()/cx() Tailwind class-merge helper (shadcn + Tremor Raw)"
    - path: ".planning/phases/00-foundation/PROVISIONING.md"
      provides: "External-service provisioning checklist (Supabase project, Google OAuth client, Vercel link, env vars)"
  key_links:
    - from: "package.json"
      to: "recharts"
      via: "Tremor Raw dependency present; @tremor/react absent (FND-06)"
      pattern: "recharts"
---

<objective>
Scaffold the Next.js 15 + Tailwind v4 + shadcn/ui application shell, install the Tremor Raw charting dependencies (Recharts, no `@tremor/react`), stand up the Vitest test infrastructure with the Wave 0 failing tests/assertions (Nyquist), and produce the external-service provisioning checklist (Supabase project, Google OAuth client, Vercel link) so Plans 02–04 have a working app, a test harness, and live credentials to build against.

This is the MVP Walking-Skeleton root task: it produces the buildable, deployable app shell and the test scaffolds that every downstream plan verifies against. No auth or schema yet — those are the next slices.

Purpose: Establish the scaffold (FND-05) and the Tremor-Raw charting wiring (FND-06), and create the test harness that makes Phase 0 Nyquist-compliant (the per-task automated verify exists from Wave 0). Per D-01..D-06.
Output: A running Next 15 app, shadcn initialized (new-york + neutral), Recharts installed, Vitest + Wave 0 test files, `.env.example`, and `PROVISIONING.md`.
</objective>

<execution_context>
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/workflows/execute-plan.md
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-VALIDATION.md

<interfaces>
<!-- Verified versions from RESEARCH.md Standard Stack (npm-confirmed 2026-06-21). -->
<!-- pnpm only (D-02), Node 20+. App name finance-bi (D-03). -->

Core deps (pin Next to 15.x — latest is 16.x, intentionally not adopted):
  next@15.x  react@19.x  react-dom@19.x  typescript@5.x  tailwindcss@4.x

Supabase clients (installed now, wired in Plans 02/03/04):
  @supabase/supabase-js@2.x  @supabase/ssr@0.12.x

Data layer (installed now, used in Plan 02):
  drizzle-orm@0.45.x  postgres@3.4.x  drizzle-kit@0.31.x (dev)

Charting (Tremor Raw — NO @tremor/react, FND-06):
  recharts@3.x  clsx@2.x  tailwind-merge@3.x  tailwind-variants@3.x

Server-only guard + validation + dates (used in Plans 02/03/04):
  server-only@0.0.1  zod@4.x  date-fns@4.x

Test infra (this plan):
  vitest (dev)

shadcn init substitution (RESEARCH A1/A2 — `default`/`slate` are removed from the CLI):
  style: new-york   base color: neutral (or zinc)   cssVariables: true
  Intent from D-04 (neutral grey, CSS variables on) is preserved.

Env-var key-style note (RESEARCH A3): a new Supabase project may issue
  sb_publishable_ / sb_secret_ keys instead of anon / service_role.
  Keep the locked env-var NAMES; map whichever key VALUE the dashboard issues.
</interfaces>
</context>

<artifacts_this_phase_produces>
<!-- New symbols/files this PHASE creates. Source-grounding passes must NOT flag these as "undefined". -->
- src/lib/utils.ts (cn/cx helper) — this plan
- vitest.config.ts, test/allowlist.test.ts, test/middleware.test.ts, test/rls.assert.sql — this plan
- src/lib/db/schema.ts, drizzle/0000_init.sql, drizzle/0001_rls_policies.sql, drizzle/0002_seed.sql, drizzle.config.ts — Plan 02
- src/lib/supabase/{client,server,service}.ts, src/lib/auth/allowlist.ts (isAllowed), src/middleware.ts, src/app/auth/callback/route.ts, src/app/(auth)/login/page.tsx, the protected page — Plans 03/04
- .github/workflows/ci.yml, ESLint service_role guard — Plan 04
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next 15 + Tailwind v4, init shadcn (new-york/neutral), install all phase deps, configure git-ignored secrets</name>
  <files>package.json, pnpm-lock.yaml, next.config.ts, tsconfig.json, components.json, src/app/globals.css, src/lib/utils.ts, .env.example, .gitignore</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md (Standard Stack table; Installation block; Pitfall 2 shadcn drift; Recommended Project Structure)
    - .claude/CLAUDE.md (stack versions, Tremor Raw rationale, env vars)
  </read_first>
  <action>
    Run `create-next-app` for app name `finance-bi` with App Router, TypeScript, Tailwind, ESLint, `src/` directory, and import alias `@/*` (D-01), using pnpm (D-02), Node 20+. Pin `next` to the 15.x line — do NOT adopt 16.x. Then `pnpm dlx shadcn@latest init` choosing style `new-york` and base color `neutral` (or `zinc`) with CSS variables ON — this substitutes for the now-removed `default`/`slate` values while preserving the D-04 intent (neutral grey, CSS variables on); record the substitution in the SUMMARY. Install all phase dependencies in one pass per the RESEARCH Installation block: `@supabase/supabase-js @supabase/ssr drizzle-orm postgres recharts clsx tailwind-merge tailwind-variants server-only zod date-fns`, and dev deps `drizzle-kit`. Do NOT install `@tremor/react` (FND-06, D-05) — Tremor Raw is copy-paste only. Ensure `src/lib/utils.ts` exports a `cn` helper (clsx + tailwind-merge) that both shadcn and Tremor Raw blocks reuse (Tremor calls it `cx`; export both names or alias). Configure light-mode only (D-06) — do not add a dark-mode toggle or `.dark` theme wiring. Create `.env.example` listing all 5 env-var NAMES (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, ALLOWED_EMAILS, DATABASE_URL) with placeholder values only. Confirm `.gitignore` ignores `.env.local` / `.env*` (never the example) so no secret is committed.
  </action>
  <verify>
    <automated>cd /Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi && pnpm install && pnpm build && grep -q '"next"' package.json && grep -q 'new-york' components.json && grep -q 'recharts' package.json && ! grep -q '@tremor/react' package.json && grep -q '.env' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` succeeds on a fresh `pnpm install`.
    - `components.json` records `style: "new-york"` and a neutral-family base color with `cssVariables: true`.
    - `package.json` contains `recharts`, `clsx`, `tailwind-merge`, `tailwind-variants`, `@supabase/ssr`, `drizzle-orm`, `postgres`, `server-only`, `zod`, `date-fns`, and `drizzle-kit` (dev); and does NOT contain `@tremor/react`.
    - `next` is pinned to a 15.x version (not 16.x).
    - `src/lib/utils.ts` exports a working class-merge helper.
    - `.env.example` lists all 5 env-var names with placeholder values; `.gitignore` ignores `.env.local`/`.env*`; no real secret is committed.
  </acceptance_criteria>
  <done>Next 15 app scaffolded, shadcn initialized (new-york/neutral), all deps installed (no @tremor/react), build green, secrets git-ignored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stand up Vitest and write the Wave 0 failing tests + SQL assertion file (Nyquist test harness)</name>
  <files>vitest.config.ts, test/allowlist.test.ts, test/middleware.test.ts, test/rls.assert.sql, package.json</files>
  <read_first>
    - .planning/phases/00-foundation/00-VALIDATION.md (Per-Requirement Verification Map; Wave 0 Requirements)
    - .planning/phases/00-foundation/00-RESEARCH.md (Validation Architecture; the dim_calendar counts ~4383 days / 144 period_keys / min-max 202401-203512; RLS assertion SQL)
  </read_first>
  <behavior>
    - test/allowlist.test.ts: `isAllowed('lorenzo@example.com')` is true when that email is in ALLOWED_EMAILS; `isAllowed('stranger@example.com')` is false; parsing is case-insensitive and trims whitespace. (Imports the not-yet-existing `@/lib/auth/allowlist` — RED until Plan 03.)
    - test/middleware.test.ts: an unauthenticated request to a protected path resolves to a 307 redirect to `/login`; an authenticated-but-non-allowlisted user is signed out and redirected to `/login?denied=1`. (Targets the not-yet-existing middleware — RED until Plan 03.)
    - test/rls.assert.sql: asserts (a) `count(*) from pg_tables where schemaname='public' and rowsecurity=false` equals 0; (b) each of the 12 tables + dim_calendar exists via `to_regclass`; (c) `count(*) from dim_calendar` is ~4383 and `count(distinct period_key)` equals 144 with min/max period_key 202401/203512; (d) `count(*) from members` equals 2; (e) categories cover the 3 groups. (Runs against the LIVE DB after Plan 02 push — pending until then.)
  </behavior>
  <action>
    Add Vitest as a dev dependency and create `vitest.config.ts` (node/jsdom env as appropriate; resolve the `@/*` alias to `src/`). Add `package.json` scripts: `test` -> `vitest run`, and `test:rls` -> a command that executes `test/rls.assert.sql` against `DATABASE_URL` (e.g. via `psql "$DATABASE_URL" -f test/rls.assert.sql` or a tiny postgres-js runner) and exits non-zero on any failed assertion. Write the three Wave-0 test/assertion files described in `<behavior>`, importing the symbols that Plans 02/03 will create (`@/lib/auth/allowlist`, the middleware). These tests are EXPECTED to be RED now — they are the executable contracts Plans 02/03 turn green; do NOT stub the production symbols here. Use `vitest run` (never watch mode). Keep grep-gate hygiene in the SQL file: comments must not contain the literal tokens a later negative grep checks for.
  </action>
  <verify>
    <automated>cd /Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi && test -f vitest.config.ts && test -f test/allowlist.test.ts && test -f test/middleware.test.ts && test -f test/rls.assert.sql && grep -q '"test"' package.json && grep -q 'test:rls' package.json && grep -q 'period_key' test/rls.assert.sql</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` exists and resolves the `@/*` alias.
    - `package.json` has `test` (`vitest run`, no watch) and `test:rls` scripts.
    - `test/allowlist.test.ts` and `test/middleware.test.ts` exist and run under `pnpm test` (RED is acceptable now — they import targets created in Plan 03).
    - `test/rls.assert.sql` asserts RLS-on-every-table, the 12+1 tables exist, the dim_calendar counts (~4383 days / 144 period_keys / 202401–203512), members=2, and the 3 category groups.
  </acceptance_criteria>
  <done>Vitest configured; the 3 Wave-0 contracts exist and execute; no watch mode; SQL assertion file encodes the FND-02/FND-04 checks.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Provision external services (Supabase project, Google OAuth client, Vercel link)</name>
  <what-built>The provisioning checklist for the three external services Phase 0 cannot create programmatically.</what-built>
  <how-to-verify>
    1. Read `.planning/phases/00-foundation/PROVISIONING.md` (created by this task — see action note below).
    2. Create the Supabase project (single prod project, D-18). Copy NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY (or sb_publishable_), SUPABASE_SERVICE_ROLE_KEY (or sb_secret_), and the Session-pooler DATABASE_URL (port 5432) into `.env.local`. Record which key STYLE the project issued (legacy anon/service_role vs new sb_publishable_/sb_secret_) per RESEARCH A3.
    3. Create the Google Cloud OAuth 2.0 Web client; add authorized redirect URIs: the Supabase callback `https://<project-ref>.supabase.co/auth/v1/callback`, the Vercel production URL, and `http://localhost:3000/auth/callback`. Paste the client id + secret into Supabase -> Authentication -> Providers -> Google and enable it.
    4. Link the GitHub repo to a new Vercel project (Hobby) and set all 5 env vars there.
    5. Set ALLOWED_EMAILS in `.env.local` to the 2 allowlisted Google emails (comma-separated, lowercase).
  </how-to-verify>
  <resume-signal>Type "provisioned" once Supabase project + Google OAuth client + Vercel link exist and `.env.local` holds all 5 values; or describe what is blocked.</resume-signal>
  <action>
    Before pausing, WRITE `.planning/phases/00-foundation/PROVISIONING.md` capturing the full checklist above (Supabase project, Google OAuth client + redirect URIs, Vercel link, the 5 env vars and where each comes from, and the A3 key-style note). This is `autonomous: false` because the credentials and OAuth client cannot be created via CLI/API by the executor — they require the developer's Google/Supabase/Vercel accounts. Plan 02 (schema push) needs DATABASE_URL; Plan 03 (auth) needs the Supabase URL/keys + the Google client; Plan 04 (deploy) needs the Vercel link.
  </action>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> git | Secrets must not cross into version control |
| dashboards -> app runtime | OAuth client + DB credentials provisioned out-of-band; only env-var NAMES live in git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | `.env.local` / real secrets | mitigate | `.gitignore` ignores `.env.local`/`.env*`; only `.env.example` (placeholders) committed; verified in Task 1 grep |
| T-00-02 | Information Disclosure | `SUPABASE_SERVICE_ROLE_KEY` naming | mitigate | env-var name is non-public (never `NEXT_PUBLIC_*`); the CI bundle grep that enforces this lands in Plan 04 |
| T-00-03 | Tampering | OAuth `redirectTo` / authorized URIs | mitigate | PROVISIONING.md restricts authorized redirect URIs to the Supabase callback + Vercel URL + localhost only (no wildcards) |
| T-00-SC | Tampering | pnpm installs (Next/shadcn/Drizzle/Supabase/Recharts) | mitigate | RESEARCH Package Legitimacy Audit cleared all packages OK (the two Supabase SUS verdicts are confirmed first-party false positives); no `[ASSUMED]`/`[SUS]` package requires a checkpoint |
</threat_model>

<verification>
- `pnpm install && pnpm build` succeeds.
- `pnpm test` runs Vitest (Wave 0 tests may be RED — they are contracts for Plans 02/03).
- `package.json` contains the Tremor Raw deps and NOT `@tremor/react` (FND-06).
- `components.json` uses `new-york` + neutral base color, cssVariables on.
- `.env.example` lists all 5 env-var names; `.gitignore` ignores `.env.local`.
- `PROVISIONING.md` exists and the human checkpoint confirms the three external services are provisioned.
</verification>

<success_criteria>
- A buildable Next 15 app shell exists (FND-05 scaffold leg) with shadcn (new-york/neutral) and the Tremor Raw charting deps (FND-06).
- Vitest + the 3 Wave-0 contracts exist and run (Nyquist harness in place).
- All five env-var names are declared and git-ignored; no secret committed.
- Supabase project, Google OAuth client, and Vercel link are provisioned (human checkpoint), unblocking Plans 02–04.
</success_criteria>

<output>
Create `.planning/phases/00-foundation/00-01-SUMMARY.md` when done. Record: the exact pinned `next` version, the shadcn substitution (new-york/neutral vs the requested default/slate), and which Supabase key STYLE the project issued (legacy vs sb_publishable_/sb_secret_) so Plans 02–04 use the right key VALUES.
</output>
