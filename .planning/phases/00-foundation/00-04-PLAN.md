---
phase: 00-foundation
plan: 04
type: execute
wave: 4
depends_on: ["00-01", "00-02", "00-03"]
files_modified:
  - src/lib/supabase/service.ts
  - eslint.config.mjs
  - .github/workflows/ci.yml
  - package.json
autonomous: false
requirements: [FND-03, FND-05]
user_setup:
  - service: vercel
    why: "Deploy the app on Hobby and verify it is reachable (FND-05); the project + env vars were linked in Plan 01"
    dashboard_config:
      - task: "Trigger the production deployment (git push to the linked branch) and copy the production URL"
        location: "Vercel Dashboard -> the linked project -> Deployments"
  - service: github
    why: "CI workflow runs lint + build + bundle grep + SQL assertions; needs repo secrets"
    env_vars:
      - name: DATABASE_URL
        source: "GitHub repo -> Settings -> Secrets and variables -> Actions (Session pooler URL from Plan 01)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "GitHub repo -> Settings -> Secrets -> Actions (for the bundle-value grep; server-only)"
      - name: ALLOWED_EMAILS
        source: "GitHub repo -> Settings -> Secrets -> Actions (for build/test env)"

must_haves:
  truths:
    - "service_role is constructed in exactly one server-only module and is unreachable from client code"
    - "CI fails the build if SUPABASE_SERVICE_ROLE_KEY (name or value) appears in the client bundle (.next/static)"
    - "CI fails if any client/UI file imports the service module or references the service-role env var"
    - "The app is deployed and reachable on Vercel; /login renders"
  artifacts:
    - path: "src/lib/supabase/service.ts"
      provides: "service_role client chokepoint, guarded by import 'server-only'"
      contains: "server-only"
      exports: ["createServiceClient"]
    - path: "eslint.config.mjs"
      provides: "no-restricted-imports (service module) + no-restricted-syntax (service-role env in client files)"
      contains: "no-restricted-imports"
    - path: ".github/workflows/ci.yml"
      provides: "lint + build + bundle grep (FND-03) + SQL/RLS assertions"
      contains: ".next/static"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".next/static"
      via: "grep over the built client bundle for the service-role key name AND value (FND-03 hard assertion)"
      pattern: "next/static"
    - from: "eslint.config.mjs"
      to: "src/lib/supabase/service.ts"
      via: "no-restricted-imports blocks the service module from client code"
      pattern: "no-restricted-imports"
---

<objective>
Isolate the `service_role` key behind a single server-only chokepoint (`lib/supabase/service.ts`), add the ESLint guard (no client import of the service module; no service-role env reference in client files), build the CI workflow whose bundle grep is the authoritative FND-03 assertion (fail the build if the key name OR value reaches `.next/static`) plus lint + build + the SQL/RLS assertions, and deploy to Vercel Hobby and verify reachability. Per D-16, D-18, D-19.

This slice closes the security keystone (FND-03) and the deploy leg of FND-05 — the last increment that makes Phase 0 verifiable end-to-end: a deployed app where an allowlisted login works, RLS holds, and CI proves the service_role never ships to the browser.

Purpose: Satisfy FND-03 (service_role server-only, CI-asserted) and FND-05 (deployed & reachable on Vercel). Layer all three guards — `server-only` (build error), ESLint (fast lint failure), and the CI bundle grep (the literal "CI-asserted" requirement).
Output: `service.ts`, the ESLint guard, `.github/workflows/ci.yml`, and a live Vercel deployment.
</objective>

<execution_context>
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/workflows/execute-plan.md
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-VALIDATION.md
@.planning/phases/00-foundation/00-01-SUMMARY.md
@.planning/phases/00-foundation/00-02-SUMMARY.md
@.planning/phases/00-foundation/00-03-SUMMARY.md

<interfaces>
<!-- From RESEARCH.md Code Examples (service chokepoint, ESLint guard, CI grep gate). -->

src/lib/supabase/service.ts:
  import 'server-only'   // build error if imported into any client bundle
  createServiceClient() -> createClient(NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY,
    { auth: { persistSession:false, autoRefreshToken:false } })
  (use the sb_secret_ key VALUE if Plan 01 SUMMARY indicates new-style keys; the env-var NAME stays the same)

eslint.config.mjs (flat config) — two rules:
  - no-restricted-imports: pattern group ['*/lib/supabase/service','**/lib/supabase/service']
    message: service_role client is server-only.
  - no-restricted-syntax on src/**/*.tsx: forbid MemberExpression process.env.SUPABASE_SERVICE_ROLE_KEY.

CI bundle gate (the FND-03 hard assertion — RESEARCH Code Examples):
  - pnpm lint   (includes the service_role import guard)
  - pnpm build
  - grep -rIl --include='*.js' -e 'SUPABASE_SERVICE_ROLE_KEY' .next/static  -> must find nothing
  - if SUPABASE_SERVICE_ROLE_KEY is set: grep -rIlF "$SUPABASE_SERVICE_ROLE_KEY" .next/static -> must find nothing
  - run the SQL/RLS assertions (pnpm test:rls) against DATABASE_URL.

Recommendation: use ALL THREE layers — server-only + ESLint + bundle grep. The bundle grep is the
one that literally satisfies "CI-asserted" in FND-03.
</interfaces>
</context>

<artifacts_this_phase_produces>
<!-- New symbols this plan creates. -->
- src/lib/supabase/service.ts (createServiceClient)
- eslint.config.mjs guard rules (no-restricted-imports / no-restricted-syntax)
- .github/workflows/ci.yml
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: service_role chokepoint (server-only) + ESLint guard blocking client reach</name>
  <files>src/lib/supabase/service.ts, eslint.config.mjs</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md (Pattern 4; Code Examples — service.ts + ESLint guard)
    - .planning/phases/00-foundation/00-01-SUMMARY.md (which service-role key style/value the project issued)
  </read_first>
  <action>
    Create `src/lib/supabase/service.ts` beginning with `import 'server-only'`, exporting `createServiceClient()` that builds a `@supabase/supabase-js` client from `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (use the secret key VALUE per Plan 01 SUMMARY; the env-var NAME stays `SUPABASE_SERVICE_ROLE_KEY`) with `auth: { persistSession:false, autoRefreshToken:false }`. This is the single module allowed to construct the elevated client (Pattern 4). Add to `eslint.config.mjs` (flat config): a `no-restricted-imports` rule whose pattern group blocks `*/lib/supabase/service` and `**/lib/supabase/service` from being imported anywhere it should not be (with a server-only message), and a `no-restricted-syntax` rule on `src/**/*.tsx` that forbids a `process.env.SUPABASE_SERVICE_ROLE_KEY` member expression in client/UI files. Confirm `pnpm lint` passes for the current (correct) codebase and would FAIL if a client file imported the service module — do not add such a violating import; the guard's job is to fail future violations.
  </action>
  <verify>
    <automated>cd /Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi && grep -q "server-only" src/lib/supabase/service.ts && grep -q 'no-restricted-imports' eslint.config.mjs && grep -q 'SUPABASE_SERVICE_ROLE_KEY' eslint.config.mjs && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `service.ts` starts with `import 'server-only'` and is the only module constructing the service_role client.
    - `eslint.config.mjs` has `no-restricted-imports` (service module) and `no-restricted-syntax` (service-role env in `.tsx`).
    - `pnpm lint` passes on the correct codebase.
  </acceptance_criteria>
  <done>service_role chokepoint exists (server-only); ESLint guard in place; lint green.</done>
</task>

<task type="auto">
  <name>Task 2: CI workflow — lint + build + bundle grep (FND-03 hard assertion) + SQL/RLS assertions</name>
  <files>.github/workflows/ci.yml, package.json</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md (Code Examples — CI grep/bundle gate; Validation Architecture Sampling Rate)
    - .planning/phases/00-foundation/00-VALIDATION.md (Full suite command; FND-03 assertion)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` running on push/PR with pnpm + Node 20: install, `pnpm lint`, `pnpm build`, then the FND-03 bundle assertion — `grep -rIl --include='*.js' -e 'SUPABASE_SERVICE_ROLE_KEY' .next/static` MUST find nothing (fail the job if it does), and when the `SUPABASE_SERVICE_ROLE_KEY` secret is available, also `grep -rIlF "$SUPABASE_SERVICE_ROLE_KEY" .next/static` MUST find nothing (assert the literal secret value never inlined). Then run `pnpm test` (Vitest) and `pnpm test:rls` against the `DATABASE_URL` repo secret (the RLS-on-every-table + zero-rows + seed-count assertions from Plan 01/02). Pull `DATABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `ALLOWED_EMAILS` from GitHub Actions secrets; never echo secret values in logs (redact). Keep grep-gate hygiene: any comment in the workflow that references the secret-key token must not be on a line a negative grep would scan as bundle output — the grep targets `.next/static` only, so workflow comments are safe, but avoid writing the literal value anywhere.
  </action>
  <verify>
    <automated>cd /Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi && test -f .github/workflows/ci.yml && grep -q '.next/static' .github/workflows/ci.yml && grep -q 'pnpm build' .github/workflows/ci.yml && grep -q 'test:rls' .github/workflows/ci.yml && grep -q 'pnpm lint' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` runs lint, build, the bundle grep over `.next/static` for both the key NAME and (when available) the key VALUE, plus `pnpm test` and `pnpm test:rls`.
    - The bundle grep fails the job if `SUPABASE_SERVICE_ROLE_KEY` (name or value) appears in `.next/static`.
    - Secrets are read from GitHub Actions secrets and never echoed.
  </acceptance_criteria>
  <done>CI workflow asserts service_role is absent from the client bundle (FND-03) and runs the full lint/build/test/RLS suite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy to Vercel and verify the auth gate + CI end-to-end</name>
  <what-built>The Vercel Hobby production deployment of the Phase-0 app shell with the auth gate, RLS-bound DB, and CI guard all wired.</what-built>
  <how-to-verify>
    1. Trigger the production deploy (push to the linked branch). Wait for Vercel to finish.
    2. `curl -I <vercel-production-url>` -> expect 200 or a redirect (the gate sends unauthenticated requests to /login).
    3. Visit `<vercel-production-url>/login` -> the Google sign-in page renders.
    4. Sign in with an ALLOWLISTED Google email -> you land on the protected page and see the value read from the seeded table (RLS-bound read works).
    5. Sign in (in a private window) with a NON-allowlisted Google email -> you are blocked/signed out and returned to /login?denied=1.
    6. Confirm the GitHub Actions CI run on the deploy commit is GREEN (lint + build + bundle grep + SQL/RLS assertions all pass) — proving service_role is not in the client bundle (FND-03).
  </how-to-verify>
  <resume-signal>Type "approved" once the deployed app is reachable, allowlisted login reaches the protected page, a non-allowlisted email is blocked, and CI is green; or describe issues.</resume-signal>
  <action>Trigger the Vercel production deployment (push to the linked branch) and confirm the auth gate end-to-end on the live URL per the how-to-verify steps; this is human-gated because real Google OAuth/SCA and a live deployment cannot be exercised by the executor. The deploy itself is automated via the Plan-01 Vercel↔GitHub link; the human verifies reachability, allowlisted login, non-allowlisted rejection, and a green CI run.</action>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-only module -> client bundle | The service_role client must never cross into `.next/static` |
| GitHub Actions -> secrets | service_role/DATABASE_URL live only as encrypted CI secrets, never in git |
| Vercel runtime -> browser | Only anon/publishable key + RLS reach the client; service_role stays server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-13 | Information Disclosure / Elevation | service_role key leaked into the client bundle | mitigate | three layers: `server-only` (build error) + ESLint `no-restricted-imports`/`no-restricted-syntax` + CI grep over `.next/static` for key NAME and VALUE (FND-03) |
| T-00-14 | Information Disclosure | Secrets committed to git or echoed in CI logs | mitigate | secrets only as GitHub Actions / Vercel encrypted secrets; `.env.local` git-ignored (Plan 01); no secret echoed in workflow steps |
| T-00-15 | Tampering | service_role named `NEXT_PUBLIC_*` (inlined into bundle) | mitigate | env-var name is `SUPABASE_SERVICE_ROLE_KEY` (non-public); ESLint forbids the env reference in `.tsx`; bundle grep is the backstop |
| T-00-SC | Tampering | CI/CD supply chain | accept | GitHub Actions on the project's own repo with pinned action versions; low-value 2-user private app, no third-party deploy hooks |
</threat_model>

<verification>
- `pnpm lint` passes; `service.ts` starts with `import 'server-only'`.
- `.github/workflows/ci.yml` runs lint + build + the `.next/static` bundle grep (name + value) + `pnpm test` + `pnpm test:rls`.
- Human checkpoint: deployed Vercel app reachable; allowlisted login reaches the protected page; non-allowlisted blocked; CI green.
</verification>

<success_criteria>
- FND-03: `service_role` is isolated to server-only code; CI fails the build if it appears in the client bundle and passes when isolated.
- FND-05: the app (Next 15 + Tailwind v4 + Tremor Raw + `@supabase/ssr`) is deployed and reachable on Vercel.
- D-19 definition of done is fully met across Plans 01-04: allowlisted Google login works and rejects others; RLS on all tables; service_role server-only (CI-asserted); full schema + seed live; Next 15 live on Vercel; a protected page renders only after an allowlisted login.
</success_criteria>

<output>
Create `.planning/phases/00-foundation/00-04-SUMMARY.md` when done. Record: the Vercel production URL, the CI run result/link, and confirmation that the bundle grep found no service_role name/value in `.next/static`.
</output>
