---
phase: 01-ingestion-enable-banking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/jwt.test.ts
  - test/dedupe.test.ts
  - test/normalize.test.ts
  - test/rules.test.ts
  - test/connect.test.ts
  - test/ingest.heartbeat.test.ts
  - test/ingest.consent.test.ts
  - test/fixtures/eb-sessions.json
  - test/fixtures/eb-transactions-page.json
  - scripts/eb-connect.ts
  - .planning/phases/01-ingestion-enable-banking/01-SPIKE.md
autonomous: false
requirements: [ING-01]
user_setup:
  - service: enable-banking
    why: "AISP open-banking access to the couple's Revolut accounts (Restricted Production — link own accounts free, no signed contract)"
    env_vars:
      - name: ENABLE_BANKING_APP_ID
        source: "Enable Banking Control Panel -> Applications -> your app -> Application ID (used as the JWT kid)"
      - name: ENABLE_BANKING_PRIVATE_KEY
        source: "Enable Banking Control Panel -> generated RSA private key (.pem, PKCS8). Paste into .env.local (multi-line) and a GitHub Secret. NEVER commit."
    dashboard_config:
      - task: "Register a Restricted Production application and generate the RSA key pair"
        location: "Enable Banking Control Panel -> Applications -> New"
      - task: "Whitelist the exact redirect_url http://localhost:3000/eb/callback (open-redirect mitigation T-01-05)"
        location: "Enable Banking Control Panel -> Applications -> your app -> Redirect URLs"

must_haves:
  truths:
    - "All Wave-0 unit test files exist and FAIL (RED) because the modules under test do not exist yet"
    - "Running pnpm eb:connect performs the real Revolut SCA once and prints the full /sessions accounts[] + access.valid_until"
    - "The spike documents whether the investing account is PSD2-exposed (resolves A2), whether creditor/debtor IBANs are returned (resolves A6), and the real consent-window value (resolves valid_until/A5)"
    - "Real Revolut /sessions and a transactions page are captured as test fixtures (PII-scrubbed)"
  artifacts:
    - path: "test/jwt.test.ts"
      provides: "RED test: signEbJwt header alg=RS256/kid=appId; exp-iat <= 86400 (ING-02)"
      contains: "signEbJwt"
    - path: "test/dedupe.test.ts"
      provides: "RED test: double-pull idempotency + hash stable across value_date flips (ING-03)"
      contains: "dedupeHash"
    - path: "test/normalize.test.ts"
      provides: "RED test: sign from credit_debit_indicator, period from booking_date, PEND excluded"
      contains: "normalize"
    - path: "test/rules.test.ts"
      provides: "RED test: investimento / transferencia / revenue / cost / sublocacao / default cost_center"
      contains: "applyRules"
    - path: "scripts/eb-connect.ts"
      provides: "One-time interactive SCA consent script (pnpm eb:connect) — spike form prints the accounts[]"
      min_lines: 40
    - path: ".planning/phases/01-ingestion-enable-banking/01-SPIKE.md"
      provides: "Documented enumeration of exposed accounts + consent window + field availability (ING-01)"
      min_lines: 30
  key_links:
    - from: "scripts/eb-connect.ts"
      to: "src/lib/ingestion/enable-banking/jwt.ts"
      via: "imports signEbJwt to sign the RS256 JWT (jwt.ts created GREEN in 01-04; spike script may inline a temporary signer)"
      pattern: "signEbJwt|SignJWT"
---

<objective>
Establish the Phase-1 Wave-0 TDD safety net and run the discovery spike (ING-01) that resolves the three research-flagged unknowns BEFORE any schema or pipeline code is written.

This is the first MVP slice: after this plan, a real human can connect the bank once and SEE exactly which Revolut accounts/pockets PSD2 exposes — the single fact the entire investimento-detection strategy branches on (D-22 / research A2). The failing unit tests freeze the behavioral contract for the pure pipeline modules (dedupe, normalize, rules, jwt) so subsequent plans implement to a fixed spec rather than inventing one.

Purpose: De-risk the phase. Two genuine unknowns (is the investing account exposed? what is the real consent window?) cannot be answered from docs — only from a live SCA run. Capturing real fixtures here lets every downstream test be mocked/offline.
Output: 7 RED Wave-0 test files, 2 real captured fixtures, the `pnpm eb:connect` script (spike form), and 01-SPIKE.md documenting the live findings.
</objective>

<execution_context>
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/workflows/execute-plan.md
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ingestion-enable-banking/01-CONTEXT.md
@.planning/phases/01-ingestion-enable-banking/01-RESEARCH.md
@.planning/phases/01-ingestion-enable-banking/01-VALIDATION.md

# Existing test conventions to mirror
@test/source-cleanliness.test.ts
@scripts/gen-calendar.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Wave-0 RED unit + integration test scaffolds (jwt, dedupe, normalize, rules, connect, heartbeat, consent)</name>
  <files>test/jwt.test.ts, test/dedupe.test.ts, test/normalize.test.ts, test/rules.test.ts, test/connect.test.ts, test/ingest.heartbeat.test.ts, test/ingest.consent.test.ts</files>
  <read_first>
    @.planning/phases/01-ingestion-enable-banking/01-VALIDATION.md (the Per-Requirement Verification Map + Wave 0 Requirements)
    @.planning/phases/01-ingestion-enable-banking/01-RESEARCH.md (Code Examples: signEbJwt, dedupeHash, normalize, applyRules signatures)
    @test/source-cleanliness.test.ts (Vitest describe/it conventions already in this repo)
  </read_first>
  <behavior>
    These tests are RED on purpose — they import modules that do not exist yet (created GREEN in 01-04). Freeze these contracts:
    - jwt.test.ts: signEbJwt(appId, privateKeyPem) -> a JWT whose decoded protected header has alg="RS256" and kid===appId; payload iss="enablebanking.com", aud="api.enablebanking.com", and exp-iat <= 86400 (24h ceiling) — assert exp-iat is exactly 3600 for a "1h" call. Decode with a base64url split (no verification needed for header/claim shape). [ING-02]
    - dedupe.test.ts: dedupeHash(normalized) is DETERMINISTIC — identical input yields identical hash; the hash is IDENTICAL when only value_date changes; the hash DIFFERS when amount, booking_date, accountId, or bankTxId changes; a row WITH a bankTxId uses strategy "bank_id", a row WITHOUT uses strategy "composite". Feeding the same batch twice and de-duplicating by hash yields the original count (zero added on the second pass). [ING-03]
    - normalize.test.ts: normalize(rawTx, accountId) sets amount NEGATIVE when credit_debit_indicator="DBDT" and POSITIVE when "CRDT"; period/bookingDate comes from booking_date NOT value_date; counterparty resolves to creditor on DBDT and debtor on CRDT; a status="PEND" raw row is EXCLUDED (filtered out / returns null) while "BOOK" is kept. [Pitfall 5 / Pitfall 2]
    - rules.test.ts: applyRules(tx, accountsById) returns flowType per the ordered first-match-wins set — an outflow whose destination matches ANY is_investment=true account -> "investimento" (D-03/D-22, source/amount agnostic); a transfer whose counterparty IBAN is one of the 3 cash accounts -> "transferencia" (D-04); a salary-signature inflow -> "revenue" (faturamento, D-18/D-26); sublet rent received -> "revenue" with costCenter "sublocacao" and sublet rent/utilities paid -> "cost" with costCenter "sublocacao" (D-25); everything else -> "cost" (custo, D-18); costCenter ALWAYS defaults to the transaction account's default_cost_center when no rule overrides (CAT-07/D-19); the credit leg landing ON an is_investment account is NEVER classified "revenue" (CAT-03). Use the enum mapping from A1/D-27: faturamento=revenue, custo=cost, transferência=transferencia, investimento=investimento.
  </behavior>
  <action>
    Write the four pure-module Vitest test files importing from the not-yet-existent module paths declared in RESEARCH §Recommended Project Structure: signEbJwt from src/lib/ingestion/enable-banking/jwt, dedupeHash from src/lib/ingestion/dedupe, normalize from src/lib/ingestion/normalize, applyRules from src/lib/ingestion/rules/engine. Encode the behaviors in the behavior block as concrete assertions. Use small inline literal fixtures for jwt/dedupe/normalize; for rules, build a fake accountsById Map with at least: a Lorenzo cash account (default_cost_center "lorenzo", an iban), a Fernanda cash account ("fernanda"), a joint/shared cash account ("shared"), and a virtual is_investment=true account (with a stored iban or counterparty signature). Generate the RS256 PEM for jwt.test.ts at test setup with node:crypto generateKeyPairSync('rsa', { modulusLength: 2048 }) exporting pkcs8 — do NOT hardcode a real key. ALSO scaffold the three integration test files as RED stubs that reference the future scripts/modules so the full Wave-0 set exists from the start: test/connect.test.ts (asserts that after a mocked POST /sessions, the persisted connections.expires_at === the response access.valid_until — ING-05), test/ingest.heartbeat.test.ts (asserts an import_batches row is written on both a zero-new-tx run and a forced-error run — ING-04), test/ingest.consent.test.ts (asserts a mocked 403 sets connections.consent_status='expired', writes import_batches.status='auth_expired', and the process exits 0 — ING-05). These three may use a `describe.todo`/skipped-with-import shape so they are RED/pending now and are filled GREEN in 01-04/01-03; they must reference the real future paths (scripts/ingest, scripts/eb-connect) so the contract is visible. These tests MUST fail or be pending at import-resolution time (modules absent) — that is the RED state. Do NOT create the source modules in this plan. Per A1/D-27 reconcile enum labels to the existing schema values (revenue/cost/investimento/transferencia); the Portuguese words are display labels only.
  </action>
  <verify>
    <automated>pnpm test -- test/jwt.test.ts test/dedupe.test.ts test/normalize.test.ts test/rules.test.ts 2>&1 | grep -qiE 'fail|cannot find module|no such file|error' && echo RED-OK</automated>
  </verify>
  <done>All four test files exist; running them fails (RED) because src/lib/ingestion/* modules do not exist yet; the assertions encode every behavior in the behavior block.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Provision the Enable Banking Restricted Production app + RSA key + whitelisted redirect_url</name>
  <action>Human-only external setup (no CLI/API). See how-to-verify for the provisioning steps.</action>
  <what-built>The Enable Banking Restricted Production app registration + RSA key + whitelisted redirect_url are external, human-only setup steps (no CLI/API). This checkpoint provisions them before the live spike can run.</what-built>
  <how-to-verify>
    1. In the Enable Banking Control Panel, register a Restricted Production application and generate the RSA key pair (download the PKCS8 .pem).
    2. Add the EXACT redirect URL `http://localhost:3000/eb/callback` to the application's whitelisted Redirect URLs (this is the open-redirect mitigation T-01-05 — it must be an exact match, no wildcards).
    3. Put ENABLE_BANKING_APP_ID and ENABLE_BANKING_PRIVATE_KEY (the multi-line .pem) into `.env.local` (already gitignored) and into GitHub repository Secrets.
    4. Confirm the private key is NOT committed: `git status` shows no .pem tracked; the source-cleanliness guard remains green.
  </how-to-verify>
  <resume-signal>Type "provisioned" once the app is registered, the key is in .env.local + GitHub Secrets, and the redirect_url is whitelisted; or describe what is blocked.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build pnpm eb:connect (spike form), run the live SCA once, capture fixtures, write 01-SPIKE.md (ING-01)</name>
  <files>scripts/eb-connect.ts, test/fixtures/eb-sessions.json, test/fixtures/eb-transactions-page.json, .planning/phases/01-ingestion-enable-banking/01-SPIKE.md, package.json</files>
  <read_first>
    @.planning/phases/01-ingestion-enable-banking/01-RESEARCH.md (Pattern 1: the verified GET /aspsps -> POST /auth -> SCA -> POST /sessions flow; Code Examples for jose signing + fetchTransactions; Open Questions 1-3)
    @scripts/gen-calendar.ts (the repo's tsx script conventions)
  </read_first>
  <action>
    Create scripts/eb-connect.ts as a tsx script and add the package.json script "eb:connect": "tsx scripts/eb-connect.ts" (also add tsx as a devDependency: pnpm add -D tsx, and pnpm add jose). The script: (1) signs a 1h RS256 JWT with jose importPKCS8 + SignJWT (kid=ENABLE_BANKING_APP_ID, iss="enablebanking.com", aud="api.enablebanking.com") reading the key/app-id from process.env (loaded via `set -a; . ./.env.local; set +a`); (2) GET /aspsps?country=DE&psu_type=personal and prints Revolut's maximum_consent_validity; (3) POST /auth with a whitelisted redirect_url of EXACTLY http://localhost:3000/eb/callback (open-redirect mitigation — the value is a constant, never built from input) + a random state uuid, prints+opens the returned url; (4) starts a one-shot localhost:3000 listener on /eb/callback to capture the ?code; (5) POST /sessions with the code and prints the FULL accounts[] (uid, iban, name, currency, cash_account_type, usage) + access.valid_until. In this spike form the script may print to stdout rather than writing the DB (the connections write is wired in 01-03 once the schema exists). Run it once against the real API (this requires the human SCA at Revolut). Capture the real /sessions JSON to test/fixtures/eb-sessions.json and one real GET /accounts/{uid}/transactions page to test/fixtures/eb-transactions-page.json, with PII scrubbed: replace real IBANs/names/amounts/descriptions with realistic-but-fake values that preserve the SHAPE (field names, BOOK/PEND, CRDT/DBDT, transaction_id vs entry_reference presence) — no real financial PII in committed fixtures (CLAUDE.md secrets/PII rule + V7). Then write 01-SPIKE.md documenting: which accounts/pockets were returned and whether the investing account is among them (resolves A2 — and therefore whether investimento is detected on the incoming leg or via a virtual is_investment row on the outgoing leg); whether creditor/debtor IBANs are present for own-account transfers (resolves A6); the real access.valid_until value + maximum_consent_validity (resolves A5/valid_until); whether transaction_id or entry_reference is the stable id (informs A3); and whether PEND rows appear (informs A4). Surface these as explicit findings — do NOT silently bake assumptions; downstream plans 01-02/01-03/01-04 read 01-SPIKE.md to branch correctly.
  </action>
  <verify>
    <automated>test -f test/fixtures/eb-sessions.json && test -f test/fixtures/eb-transactions-page.json && test -f .planning/phases/01-ingestion-enable-banking/01-SPIKE.md && grep -qiE 'is_investment|investing account|valid_until|exposed' .planning/phases/01-ingestion-enable-banking/01-SPIKE.md && echo SPIKE-OK</automated>
  </verify>
  <done>pnpm eb:connect runs the real SCA once and prints the accounts[]; real fixtures captured (PII-scrubbed); 01-SPIKE.md documents exposed accounts (A2), IBAN availability (A6), and the real consent window (A5); no real PII/secret committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Revolut SCA browser → local listener | The redirect carries the consent `code` to http://localhost:3000/eb/callback — untrusted query input crosses here |
| Enable Banking API → local script | The /aspsps and /sessions responses are external, untrusted JSON |
| Local script → git repo | The private key + real financial PII must never cross into committed files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | ENABLE_BANKING_PRIVATE_KEY (.pem) | mitigate | Key lives only in .env.local (gitignored) + GitHub Secret; never echoed; the Phase-0 source-cleanliness guard test stays green |
| T-01-02 | Information Disclosure | Captured fixtures (real IBANs/amounts/descriptions) | mitigate | Scrub all financial PII before committing — fixtures preserve shape with fake values only (V7 / CLAUDE.md PII rule) |
| T-01-05 | Tampering | EB redirect_url (open redirect) | mitigate | redirect_url is the exact constant http://localhost:3000/eb/callback, whitelisted in the EB Control Panel; never constructed from input |
| T-01-06 | Tampering | /sessions + /aspsps response | mitigate | Treat as untrusted; zod-validation of these payloads is added GREEN in 01-03/01-04 (this spike only prints/scrubs them) |
| T-01-SC | Tampering | npm installs (jose, tsx) | mitigate | RESEARCH Package Legitimacy Audit verdicts: jose OK, tsx SUS(too-new)→OK false positive; no [ASSUMED]/[SUS] requiring a blocking checkpoint |
</threat_model>

<verification>
- `pnpm test -- test/jwt.test.ts test/dedupe.test.ts test/normalize.test.ts test/rules.test.ts` fails RED (modules absent) — proves the contracts are frozen before implementation.
- `pnpm eb:connect` completes the real SCA once and prints the accounts[] + valid_until.
- Fixtures + 01-SPIKE.md exist; `git status` shows no .pem and the source-cleanliness test is green.
</verification>

<success_criteria>
- 7 Wave-0 test files exist (4 written here as RED; the 3 integration tests connect.test.ts / ingest.heartbeat.test.ts / ingest.consent.test.ts are scaffolded as RED stubs referencing the future modules).
- ING-01 satisfied: the spike documents the exposed-account enumeration + real consent window + IBAN-field availability.
- No secret or real financial PII committed.
</success_criteria>

<output>
Create `.planning/phases/01-ingestion-enable-banking/01-01-SUMMARY.md` when done. In it, record the resolved A2 (investing account exposed? incoming-leg vs virtual-row detection), A6 (IBANs present?), and the real valid_until — these gate 01-02/01-03/01-04.
</output>
