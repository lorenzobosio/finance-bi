---
phase: 01-ingestion-enable-banking
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0003_ingestion.sql
  - drizzle/0004_ingestion_rls.sql
  - test/rls.assert.mjs
autonomous: false
requirements: [ING-03, ING-04, CAT-01, CAT-07]
must_haves:
  truths:
    - "accounts has is_investment, enable_banking_id, iban, is_synced; transactions has description_raw, counterparty, counterparty_iban, is_recurring, status; connections has consent_status, last_pull_at, session_id"
    - "A cost_centers lookup table exists (FK-referenced), seeded lorenzo/fernanda/compartilhado/sublocacao, so new cost centers add without a breaking migration (D-24)"
    - "import_batches table exists with RLS ENABLED + the allowlist policy in the SAME migration"
    - "The migration is APPLIED to the live Supabase DB — all new columns/tables actually exist and RLS is ON before the phase proceeds"
    - "test:rls asserts rowsecurity=true for import_batches and cost_centers and that a non-allowlisted JWT sees zero rows"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Extended Drizzle schema: new columns + importBatches + costCenters tables"
      contains: "importBatches"
    - path: "drizzle/0003_ingestion.sql"
      provides: "Generated DDL: new columns + import_batches + cost_centers"
      contains: "import_batches"
    - path: "drizzle/0004_ingestion_rls.sql"
      provides: "Custom RLS: enable + allowlist policy on import_batches and cost_centers"
      contains: "enable row level security"
    - path: "test/rls.assert.mjs"
      provides: "Extended RLS assertions covering the new tables"
      contains: "import_batches"
  key_links:
    - from: "drizzle/0004_ingestion_rls.sql"
      to: "drizzle/0001_rls_policies.sql"
      via: "reuses the same public.is_email_allowed((select auth.jwt() ->> 'email')) allowlist policy — no hardcoded emails"
      pattern: "is_email_allowed"
    - from: "src/lib/db/schema.ts"
      to: "drizzle/0003_ingestion.sql"
      via: "pnpm db:generate emits the DDL from the schema edit"
      pattern: "import_batches|cost_centers"
---

<objective>
Extend the Phase-0 schema with everything ingestion needs, make cost_center extensible via a lookup table (D-24), add the import_batches audit/heartbeat table with RLS, and APPLY it all to the live Supabase database.

This is the data-foundation slice: after this plan, the real database physically has the columns and tables the connect/ingest/banner slices write to and read from — verified by a live migration push and an extended RLS assertion, not just a schema file. Nothing downstream can land a row until these exist.

Purpose: Phase 1 is additive but introduces a new privileged write target (import_batches) and a structural change (cost_centers off the fixed enum). Both must ship with RLS in the same migration (Phase-0 posture) and be live-verified, or the whole pipeline writes into a schema that does not exist.
Output: extended schema.ts, drizzle/0003 (generated) + 0004 (RLS), an extended test/rls.assert.mjs, and a LIVE-applied migration.
</objective>

<execution_context>
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/workflows/execute-plan.md
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ingestion-enable-banking/01-CONTEXT.md
@.planning/phases/01-ingestion-enable-banking/01-RESEARCH.md
@.planning/phases/01-ingestion-enable-banking/01-SPIKE.md

# Existing schema + migration + RLS conventions to EXTEND (do not recreate)
@src/lib/db/schema.ts
@drizzle/0001_rls_policies.sql
@test/rls.assert.mjs
@drizzle.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend schema.ts with ingestion columns, the cost_centers lookup, and import_batches; generate the DDL</name>
  <files>src/lib/db/schema.ts, drizzle/0003_ingestion.sql, drizzle/meta/_journal.json</files>
  <read_first>
    @src/lib/db/schema.ts (the full existing schema — extend, never recreate; note transactions.dedupeHash UNIQUE, importBatchId is currently text, costCenter is a pgEnum)
    @.planning/phases/01-ingestion-enable-banking/01-RESEARCH.md (§Schema Migration table — exact columns + the import_batches shape; A1/D-27 enum note)
    @.planning/phases/01-ingestion-enable-banking/01-SPIKE.md (whether the investing account is exposed -> whether a virtual is_investment row is needed; whether IBANs are returned -> whether iban/counterparty_iban matching is viable)
  </read_first>
  <behavior>
    After db:generate, drizzle/0003_ingestion.sql must add (per RESEARCH §Schema Migration):
    - accounts: is_investment boolean NOT NULL DEFAULT false (the D-01 investing-account flag); enable_banking_id text nullable UNIQUE; iban text nullable; is_synced boolean NOT NULL DEFAULT true.
    - transactions: description_raw text; counterparty text; counterparty_iban text; is_recurring boolean NOT NULL DEFAULT false; status text nullable.
    - connections: consent_status text nullable; last_pull_at timestamptz nullable; session_id text nullable.
    - NEW cost_centers lookup table: code text PRIMARY KEY (lorenzo|fernanda|compartilhado|sublocacao), label text, plus FKs from transactions.cost_center and accounts.default_cost_center referencing it (D-24 — see the action for the enum-migration approach). The fixed category taxonomy (group=essential|desire|investment) is sufficient for ingest-time rules (D-20); rich category management is Phase 2.
    - NEW import_batches table: id uuid pk, started_at timestamptz, finished_at timestamptz, status text, source text, fetched int, inserted int, skipped int, error text.
  </behavior>
  <action>
    Edit src/lib/db/schema.ts to add the columns + tables in the behavior block, mirroring the existing pgTable style. For cost_centers (D-24): the existing schema uses a Postgres `cost_center` pgEnum (lorenzo/fernanda/shared) referenced by transactions.costCenter, accounts.defaultCostCenter, rules.setCostCenter, budgets.costCenter. Replace the fixed enum with an extensible lookup: add a costCenters pgTable keyed by a text `code`, seeded lorenzo/fernanda/compartilhado/sublocacao, and convert the four enum-typed columns to text FKs referencing costCenters.code. IMPORTANT: a Postgres enum→text-FK conversion is a real migration — let pnpm db:generate emit it; if drizzle-kit cannot auto-derive the enum drop+recolumn cleanly, hand-author the ALTER in 0003 (drop the enum default, ALTER COLUMN ... TYPE text USING (col::text), add the FK) and keep the values consistent with the seed. Map the existing Phase-0 `shared` value to the new `compartilhado` code (D-24 names them in Portuguese) — preserve any existing rows by translating shared->compartilhado in the same migration. Keep transactions.importBatchId — store the import_batches uuid as text (low-risk; no FK rewrite, per RESEARCH §Schema Migration note). Per A1/D-27 do NOT rename the flow_type enum — keep revenue/cost/investimento/transferencia (Portuguese are display labels). Run `pnpm db:generate` to emit drizzle/0003_ingestion.sql. Verify the generated SQL adds every column/table; do not hand-edit the generated file except for the enum-conversion ALTER if drizzle could not derive it.
  </action>
  <verify>
    <automated>pnpm db:generate && grep -qE 'import_batches' drizzle/0003_ingestion.sql && grep -qE 'cost_centers' drizzle/0003_ingestion.sql && grep -qE 'is_investment' drizzle/0003_ingestion.sql && pnpm build && echo OK</automated>
  </verify>
  <done>schema.ts has the new columns + costCenters + importBatches tables; drizzle/0003_ingestion.sql contains them; the project still type-checks/builds.</done>
</task>

<task type="auto">
  <name>Task 2: Author the RLS migration for the new tables and extend test:rls</name>
  <files>drizzle/0004_ingestion_rls.sql, test/rls.assert.mjs</files>
  <read_first>
    @drizzle/0001_rls_policies.sql (the EXACT allowlist policy every table uses — copy it verbatim for the new tables; note grants are already schema-wide so future tables inherit them, but the new tables still each need ENABLE ROW LEVEL SECURITY + a policy)
    @test/rls.assert.mjs (the DATA_TABLES list + the rowsecurity=false count assertion to extend)
  </read_first>
  <action>
    Create drizzle/0004_ingestion_rls.sql that, for BOTH new tables (import_batches and cost_centers): `alter table public.<t> enable row level security;` then the identical allowlist policy used in 0001 — `create policy "allowlist_all" on public.<t> for all to authenticated using ( public.is_email_allowed((select auth.jwt() ->> 'email')) ) with check ( public.is_email_allowed((select auth.jwt() ->> 'email')) );` Use `--> statement-breakpoint` separators to match the repo convention. Do NOT hardcode any email — reuse the Phase-0 SECURITY DEFINER public.is_email_allowed() oracle. New columns on existing tables inherit their table's existing RLS, so only the two new tables need policies. Then extend test/rls.assert.mjs: add 'import_batches' and 'cost_centers' to the DATA_TABLES array (so the existing rowsecurity=false count, the to_regclass existence loop, and the grants all cover them) and add an assertion that cost_centers is seeded with exactly the 4 codes (lorenzo, fernanda, compartilhado, sublocacao). Keep the file PII-free (no email literals).
  </action>
  <verify>
    <automated>grep -qE 'public.import_batches enable row level security' drizzle/0004_ingestion_rls.sql && grep -qE 'public.cost_centers enable row level security' drizzle/0004_ingestion_rls.sql && grep -qE "'import_batches'" test/rls.assert.mjs && grep -qE "'cost_centers'" test/rls.assert.mjs && echo OK</automated>
  </verify>
  <done>0004_ingestion_rls.sql enables RLS + the allowlist policy on import_batches and cost_centers; test/rls.assert.mjs covers both new tables and asserts the 4 seeded cost-center codes; no hardcoded emails.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply the migration to the live Supabase DB and verify with test:rls</name>
  <files>drizzle/0003_ingestion.sql, drizzle/0004_ingestion_rls.sql</files>
  <read_first>
    @drizzle.config.ts (session pooler 5432; load .env.local into the shell before running)
    @.claude/CLAUDE.md (DB-over-env; never print DATABASE_URL or secrets)
  </read_first>
  <action>
    [BLOCKING] Apply the Phase-1 migration to the LIVE Supabase database. Run, exactly: `set -a; . ./.env.local; set +a; pnpm drizzle-kit migrate` (this applies 0003 then the custom 0004 RLS migration via the session pooler on 5432). drizzle-kit migrate applies generated migrations; if the custom 0004 RLS file is not in the drizzle journal, apply it explicitly the same way the Phase-0 0001/0002 hand-written SQL migrations were applied (via the postgres driver against DATABASE_URL, or by adding it to the journal) — match whatever mechanism Phase 0 used for 0001_rls_policies.sql / 0002_seed.sql so the live DB ends up with the RLS policy. NEVER echo DATABASE_URL or any secret. After applying, run `set -a; . ./.env.local; set +a; pnpm test:rls` and confirm it passes: every public table (now including import_batches and cost_centers) has rowsecurity=true, the new tables exist, and cost_centers has the 4 seeded codes. This task is autonomous:false because it needs the live DB credentials from .env.local interactively. Tables/columns MUST actually exist + RLS ON before the phase can proceed.
  </action>
  <verify>
    <automated>set -a; . ./.env.local; set +a; pnpm test:rls 2>&1 | grep -qiE 'passed' && echo LIVE-OK</automated>
  </verify>
  <done>The live Supabase DB has all new columns + import_batches + cost_centers; RLS is enabled with the allowlist policy on both new tables; pnpm test:rls passes against the live DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration runner → live Supabase | DDL + RLS DDL applied with elevated DB credentials (DATABASE_URL) |
| App (user JWT) → import_batches / cost_centers | Allowlisted reads of audit + lookup data under RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Elevation of Privilege | import_batches (new write target) | mitigate | RLS ENABLED + allowlist policy in the SAME migration (0004); cron writes via service_role (server-only), app reads under user JWT |
| T-01-08 | Information Disclosure | DATABASE_URL during migrate | mitigate | Loaded from .env.local into the shell only; never echoed/logged (drizzle.config.ts rule) |
| T-01-09 | Tampering | enum→FK conversion data loss | mitigate | Preserve existing rows by translating shared->compartilhado in the same migration; live test:rls confirms tables/seed |
| T-01-SC | Tampering | npm/drizzle-kit | accept | No new third-party installs in this plan; drizzle-kit already vetted in Phase 0 |
</threat_model>

<verification>
- `pnpm db:generate` emits 0003 with all new columns/tables; `pnpm build` clean.
- 0004 enables RLS + allowlist on import_batches and cost_centers using is_email_allowed (no hardcoded emails).
- Live: `pnpm drizzle-kit migrate` applied; `pnpm test:rls` passes (every public table rowsecurity=true, new tables exist, cost_centers seeded with 4 codes).
</verification>

<success_criteria>
- ING-03 (UNIQUE dedupe_hash already exists; ingestion columns now present), ING-04 (import_batches table live + RLS), CAT-01 (taxonomy sufficient; cost_centers extensible), CAT-07 (default_cost_center now FK to cost_centers) are structurally satisfied at the schema layer.
- The live DB physically matches the schema; no email literal in any migration.
</success_criteria>

<output>
Create `.planning/phases/01-ingestion-enable-banking/01-02-SUMMARY.md` when done. Record the final cost_center representation (lookup table + the shared->compartilhado translation) and confirm the live migration + test:rls outcome.
</output>
