---
phase: 01-ingestion-enable-banking
plan: 04
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - src/lib/ingestion/normalize.ts
  - src/lib/ingestion/dedupe.ts
  - src/lib/ingestion/rules/engine.ts
  - src/lib/ingestion/rules/builtins.ts
  - scripts/ingest.ts
  - test/normalize.test.ts
  - test/dedupe.test.ts
  - test/rules.test.ts
  - test/ingest.heartbeat.test.ts
  - test/ingest.consent.test.ts
autonomous: true
requirements: [ING-02, ING-03, ING-04, ING-05, CAT-02, CAT-03, CAT-07]
must_haves:
  truths:
    - "normalize derives sign from credit_debit_indicator (DBDT negative), period from booking_date, and excludes PEND — normalize.test.ts GREEN"
    - "dedupeHash is deterministic, prefers the bank id, falls back to a composite pinned to booking_date, and is stable across value_date flips — dedupe.test.ts GREEN"
    - "applyRules classifies investimento (destination = ANY is_investment account, D-03/D-22), transferencia (cash<->cash, D-04), revenue (salary, D-26), sublocacao tagging (D-25), default cost (D-18), default cost_center per account (CAT-07) — rules.test.ts GREEN"
    - "scripts/ingest.ts pulls transactions since last_pull_at + a balances snapshot, upserts ON CONFLICT (dedupe_hash) DO NOTHING (zero dups on re-pull), applies rules at write time stamping flow_type/cost_center/category/is_recurring/rule_id, writes an import_batches heartbeat in finally, and on 403 sets consent_status='expired' + exits 0"
    - "The €4k contribution is classified investimento, its credit leg is never revenue, and SUM(amount_eur) WHERE flow_type='investimento' counts it exactly once (CAT-03/D-23). The monthly-streak rollup (D-05) and the cost-basis total-invested denominator (D-06) are DERIVED from this classified transactions data in Phase 3 — Phase 1 only guarantees correct classification so those rollups are possible; it does not build them or populate investment_contributions (D-23)"
  artifacts:
    - path: "src/lib/ingestion/normalize.ts"
      provides: "rawTx -> Normalized (signed EUR, booking_date, counterparty, description_raw); PEND filtered"
      exports: ["normalize"]
    - path: "src/lib/ingestion/dedupe.ts"
      provides: "dedupeHash(Normalized) -> {hash, strategy}; versioned, node:crypto sha256"
      exports: ["dedupeHash"]
    - path: "src/lib/ingestion/rules/engine.ts"
      provides: "applyRules(tx, accountsById) -> Classification (pure, ordered, first-match-wins)"
      exports: ["applyRules"]
    - path: "src/lib/ingestion/rules/builtins.ts"
      provides: "The seeded Phase-1 rules (investimento/transferencia/revenue/sublocacao/cost) + rule ids/versions"
    - path: "scripts/ingest.ts"
      provides: "Headless daily pull: fetch -> normalize -> dedupe -> upsert -> classify -> balances -> heartbeat"
      min_lines: 80
  key_links:
    - from: "scripts/ingest.ts"
      to: "src/lib/ingestion/dedupe.ts"
      via: "computes dedupe_hash per row before the ON CONFLICT DO NOTHING upsert"
      pattern: "dedupeHash"
    - from: "scripts/ingest.ts"
      to: "src/lib/ingestion/rules/engine.ts"
      via: "applyRules stamps flow_type/cost_center/category/is_recurring/rule_id at write time"
      pattern: "applyRules"
    - from: "scripts/ingest.ts"
      to: "src/lib/supabase/service.ts"
      via: "writes transactions/balances/import_batches/connections via service_role"
      pattern: "createServiceClient"
    - from: "scripts/ingest.ts"
      to: "src/lib/ingestion/enable-banking/client.ts"
      via: "reads session from connections, paginates transactions, throws ConsentExpiredError on 403"
      pattern: "fetchTransactions|ConsentExpiredError"
---

<objective>
Implement the three pure pipeline modules (normalize, dedupe, rules) — making the remaining Wave-0 unit tests GREEN — and the headless `scripts/ingest.ts` that performs an idempotent, classify-on-ingest daily pull.

This is the correctness slice: after this plan, running `pnpm ingest` against the live session pulls new transactions, lands them already classified, snapshots balances, and is provably safe to re-run (zero duplicate rows). The €4k contribution is classified `investimento` at the source and is structurally excluded from cost/revenue — the make-or-break Phase-1 contract. A 403 surfaces as a loud `consent_status='expired'` rather than a silent freeze.

Purpose: The hard parts here are determinism (stable hash, frozen normalization, sign-from-indicator) and fail-soft observability (heartbeat, consent classification) — exactly what the Wave-0 tests pin down. The DB UNIQUE(dedupe_hash) + ON CONFLICT DO NOTHING is the real idempotency safety net.
Output: normalize.ts, dedupe.ts, rules/engine.ts, rules/builtins.ts, scripts/ingest.ts, and GREEN normalize/dedupe/rules/heartbeat/consent tests.
</objective>

<execution_context>
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/workflows/execute-plan.md
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-ingestion-enable-banking/01-CONTEXT.md
@.planning/phases/01-ingestion-enable-banking/01-RESEARCH.md
@.planning/phases/01-ingestion-enable-banking/01-SPIKE.md

# The RED tests to make GREEN + the client/service/schema to wire
@test/normalize.test.ts
@test/dedupe.test.ts
@test/rules.test.ts
@test/ingest.heartbeat.test.ts
@test/ingest.consent.test.ts
@src/lib/ingestion/enable-banking/client.ts
@src/lib/supabase/service.ts
@src/lib/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement normalize + dedupe (make normalize.test.ts + dedupe.test.ts GREEN)</name>
  <files>src/lib/ingestion/normalize.ts, src/lib/ingestion/dedupe.ts, test/normalize.test.ts, test/dedupe.test.ts</files>
  <read_first>
    @.planning/phases/01-ingestion-enable-banking/01-RESEARCH.md (Code Examples: normalize + dedupeHash; Pattern 2 hash composition + the frozen-normalization contract; Pitfall 5/2)
    @test/normalize.test.ts (the RED contract)
    @test/dedupe.test.ts (the RED contract)
  </read_first>
  <behavior>
    - normalize(raw, accountId): magnitude = Number(transaction_amount.amount); signed = DBDT ? -magnitude : magnitude (outflow negative); counterparty name/iban from creditor on DBDT, debtor on CRDT; descriptionRaw = remittance_information joined+trimmed; normalizedDescription = lowercased, whitespace-collapsed; bookingDate = raw.booking_date (NEVER value_date); bankTxId = transaction_id ?? entry_reference ?? null. A status="PEND" row is excluded (the function/caller filters it). normalize.test.ts GREEN.
    - dedupeHash(normalized) -> {hash, strategy}: HASH_VERSION="v1"; if bankTxId present -> sha256("v1|id|accountId|bankTxId"), strategy "bank_id"; else -> sha256("v1|composite|accountId|bookingDate|amount.toFixed(2)|normalizedDescription"), strategy "composite". Identical input -> identical hash; identical when only value_date changes; differs on amount/booking_date/accountId/bankTxId. dedupe.test.ts GREEN. The normalization + HASH_VERSION are frozen (changing them is a migration).
  </behavior>
  <action>
    Create src/lib/ingestion/normalize.ts and src/lib/ingestion/dedupe.ts exactly per the RESEARCH Code Examples. Use node:crypto createHash('sha256') for the hash (no npm hashing lib). Both are pure (no DB, no network). Make test/normalize.test.ts and test/dedupe.test.ts pass. Do not branch on the bank's amount sign anywhere — sign comes only from credit_debit_indicator.
  </action>
  <verify>
    <automated>pnpm test -- test/normalize.test.ts test/dedupe.test.ts && echo NORM-DEDUPE-GREEN</automated>
  </verify>
  <done>normalize.ts + dedupe.ts implemented; both test files GREEN; PEND excluded; hash stable across value_date, versioned, bank-id-preferred.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the versioned, ordered rules engine + builtins (make rules.test.ts GREEN)</name>
  <files>src/lib/ingestion/rules/engine.ts, src/lib/ingestion/rules/builtins.ts, test/rules.test.ts</files>
  <read_first>
    @.planning/phases/01-ingestion-enable-banking/01-RESEARCH.md (Pattern 6: the ordered rule set; Code Examples: applyRules; the A1/D-27 enum mapping; Pitfall 1)
    @.planning/phases/01-ingestion-enable-banking/01-SPIKE.md (whether the investing account is exposed -> incoming-leg vs virtual-row matching; whether IBANs are present, A6)
    @test/rules.test.ts (the RED contract)
    @.planning/phases/01-ingestion-enable-banking/01-CONTEXT.md (D-18/D-19/D-22/D-24/D-25/D-26 — the exact classification rules)
  </read_first>
  <behavior>
    applyRules(tx, accountsById) returns {flowType, costCenter, categoryId, isRecurring, ruleId}, first-match-wins by priority:
    1. investimento (highest): outflow whose destination resolves to ANY is_investment=true account — by creditor IBAN/enable_banking_id if exposed, else by the virtual account's stored IBAN/counterparty signature (D-03/D-22). Source-agnostic, amount-agnostic — the €4k may arrive from any cash account in one or more transfers (D-02). flowType="investimento".
    2. transferencia: counterparty IBAN ∈ the 3 cash accounts' IBANs (D-04). flowType="transferencia".
    3. revenue: salary/employer inflow signature (CRDT + counterparty/remittance keyword, seeded from the spike), incl. bonuses; sublet rent RECEIVED -> revenue with costCenter override "sublocacao" (D-25/D-26). flowType="revenue".
    4. cost (default, D-18): everything else; sublet rent/utilities PAID -> cost with costCenter "sublocacao" (D-25). flowType="cost".
    Independent of flowType: costCenter defaults to accountsById.get(tx.accountId).default_cost_center (CAT-07/D-19) unless a rule overrides (e.g. sublocacao); isRecurring is a conservative heuristic (default false; true for known recurring signatures); categoryId from the matched rule's set_category else null. The credit leg landing on an is_investment account is NEVER "revenue". Use enum values revenue/cost/investimento/transferencia (A1/D-27 — Portuguese are display labels). rules.test.ts GREEN.
  </behavior>
  <action>
    Create src/lib/ingestion/rules/engine.ts exporting the pure applyRules per the RESEARCH Code Example, and rules/builtins.ts holding the seeded ordered Phase-1 rules with stable rule ids + a version (so a transaction records which rule/version classified it — D-17). Branch the investimento detection on the spike's A2 finding: if the investing account is exposed, match the incoming leg / creditor IBAN; if not, match the outgoing leg against the virtual is_investment account's stored IBAN or counterparty/description signature. Implement the sublocacao tagging (D-25) as rule overrides on costCenter. Keep everything pure and deterministic. Make test/rules.test.ts pass.
  </action>
  <verify>
    <automated>pnpm test -- test/rules.test.ts && echo RULES-GREEN</automated>
  </verify>
  <done>engine.ts + builtins.ts implemented; rules.test.ts GREEN covering investimento/transferencia/revenue/sublocacao/cost + default cost_center; investimento keys on is_investment=true (never a hardcoded id); credit leg never revenue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement scripts/ingest.ts (idempotent pull + classify + heartbeat + fail-soft); make heartbeat/consent tests GREEN</name>
  <files>scripts/ingest.ts, package.json, test/ingest.heartbeat.test.ts, test/ingest.consent.test.ts</files>
  <read_first>
    @.planning/phases/01-ingestion-enable-banking/01-RESEARCH.md (Code Examples: the idempotent upsert + heartbeat-in-finally; Pattern 3/4/5; the ON CONFLICT DO NOTHING semantics)
    @src/lib/ingestion/enable-banking/client.ts (fetchTransactions/balances/ConsentExpiredError)
    @src/lib/supabase/service.ts (the service_role writer)
    @test/ingest.heartbeat.test.ts (RED: heartbeat row every run)
    @test/ingest.consent.test.ts (RED: 403 -> expired, exit 0)
  </read_first>
  <behavior>
    scripts/ingest.ts (run by `pnpm ingest` and the cron): signs a 1h JWT; reads connections.session_id + last_pull_at + expires_at; for each account uid, fetchTransactions(date_from = last_pull_at minus a small overlap — incremental since the last successful pull, D-12) paginated by continuation_key + a balances snapshot per account -> balances (D-12); zod-validated rows are normalized (PEND excluded), dedupe-hashed, and upserted ON CONFLICT (dedupe_hash) DO NOTHING (re-pull adds zero rows); applyRules stamps flow_type/cost_center/category_id/is_recurring/rule_id on the inserted rows; a balances snapshot upserts ON CONFLICT (account_id, as_of_date); an import_batches heartbeat row (status success|empty|auth_expired|error, counts) is written in a finally so it happens EVERY run incl. zero-tx/failed (the keep-alive, D-13); a failed/empty run records its status with no silent crash (D-14); on ConsentExpiredError -> connections.consent_status='expired', batch status 'auth_expired', process exits 0 (banner is the alert); on transient error -> status 'error', exit 1; on success -> advance connections.last_pull_at. Forward-only — no backfill (D-14). Logs only counts/status (V7).
    - ingest.heartbeat.test.ts GREEN: a zero-new-tx run AND a forced-error run each leave an import_batches row.
    - ingest.consent.test.ts GREEN: a mocked 403 sets consent_status='expired', writes batch status 'auth_expired', and exits 0.
  </behavior>
  <action>
    Create scripts/ingest.ts per the RESEARCH Code Example (the try/catch/finally heartbeat structure) and add the package.json script "ingest": "tsx scripts/ingest.ts". Use createServiceClient() for all writes. Structure the script so the fetch layer is injectable (so the tests can mock fetch/the client) — e.g. accept an injected client or read a module-level fetch the test can stub. Implement the incremental date_from from connections.last_pull_at (with a small overlap window so a delayed catch-up run is safe — idempotency makes overlap harmless). Make test/ingest.heartbeat.test.ts and test/ingest.consent.test.ts GREEN (they use a mocked fetch + a Supabase test write or an injected writer per the existing test style). Never log full descriptions/amounts/IBANs/keys. Do NOT import anything from scripts/ or src/lib/ingestion/ into the Next app bundle.
  </action>
  <verify>
    <automated>pnpm test -- test/ingest.heartbeat.test.ts test/ingest.consent.test.ts && grep -qE 'onConflict|ON CONFLICT|ignoreDuplicates' scripts/ingest.ts && grep -qE 'finally' scripts/ingest.ts && echo INGEST-GREEN</automated>
  </verify>
  <done>ingest.ts pulls incrementally, upserts idempotently (zero dups on re-pull), classifies on ingest, snapshots balances, writes a heartbeat every run, and fails soft on 403 (exit 0); both integration tests GREEN; pnpm ingest script registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EB API → ingest.ts | Untrusted external transaction/balance JSON |
| ingest.ts → Postgres (service_role) | Privileged write of financial rows + audit |
| dedupe/normalize determinism | A drift here silently duplicates or drops money rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Tampering | dedupe replay -> dup/drop rows | mitigate | Deterministic versioned dedupeHash + DB UNIQUE(dedupe_hash) + ON CONFLICT DO NOTHING (idempotency invariant, ING-03) |
| T-01-14 | Integrity | €4k leaking into cost/revenue or double-count | mitigate | investimento rule (highest priority, destination=ANY is_investment, D-03/D-22); €100k = SUM WHERE flow_type='investimento' from transactions only (D-23) |
| T-01-12 | Availability/Integrity | Silent consent expiry / silent retry | mitigate | ConsentExpiredError -> consent_status='expired', batch 'auth_expired', exit 0; never silent retry (Pattern 4) |
| T-01-15 | DoS | cron crash on empty/failed day | mitigate | import_batches heartbeat written in finally (keep-alive, ING-04) |
| T-01-11 | Information Disclosure | PII/secrets in CI logs | mitigate | Log only counts/hashes/status; never full descriptions/amounts/IBANs/key (V7) |
| T-01-16 | Information Disclosure | ingestion code in the client bundle | mitigate | scripts/ + src/lib/ingestion/ never imported into the Next app; service.ts is server-only |
| T-01-SC | Tampering | npm installs | accept | No new package installs in this plan — jose/tsx vetted + installed in 01-01 (RESEARCH Package Legitimacy Audit); node:crypto is a built-in |
</threat_model>

<verification>
- `pnpm test` GREEN for normalize/dedupe/rules/ingest.heartbeat/ingest.consent (the full Wave-0 unit + integration set now passes).
- `pnpm build` clean; no ingestion module in `.next/static`.
- Manual (phase gate): `pnpm ingest` twice over an overlapping window -> 2nd run inserts 0; transactions land classified; a balances snapshot + an import_batches row exist; last_pull_at advances; a forced 403 sets consent_status='expired' and exits 0.
</verification>

<success_criteria>
- ING-02/03/04/05 + CAT-02/03/07 satisfied: idempotent classify-on-ingest pull with heartbeat and fail-soft consent; the €4k is investimento, excluded from cost/revenue, counted once.
</success_criteria>

<output>
Create `.planning/phases/01-ingestion-enable-banking/01-04-SUMMARY.md` when done. Record the final dedupe strategy mix observed on a live double-pull (bank_id vs composite, ON CONFLICT match rate) and the investimento-detection branch actually used (incoming-leg vs virtual-row).
</output>
