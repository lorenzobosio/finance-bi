---
phase: 02-core-bi-house-as-business
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/format.ts
  - src/lib/period.ts
  - test/format.test.ts
  - test/period.test.ts
  - test/rules-db.test.ts
  - test/marts.test.ts
  - test/reapply.test.ts
  - test/actions.test.ts
autonomous: true
requirements: [BI-04, BI-05]
must_haves:
  truths:
    - "formatEUR(5038) renders the de-DE string €5.038,00 (period thousands, comma decimal)"
    - "formatPct(12.4) renders 12,4 % (one decimal, German space before %)"
    - "isProvisional returns true only for the period_key equal to the injected now's month"
    - "periodKeyForYoY(202606) returns 202506 (period_key minus 100)"
    - "hasYoYHistory returns false until ~12 populated periods exist"
    - "All six Wave-0 test files exist and run; the formula/helper suites pass, the marts/rules-db/reapply/actions stubs fail RED until their modules land"
  artifacts:
    - path: "src/lib/format.ts"
      provides: "formatEUR/formatPct de-DE single source of truth"
      exports: ["formatEUR", "formatPct"]
      min_lines: 15
    - path: "src/lib/period.ts"
      provides: "period_key + provisional/YoY-history pure helpers (injected now)"
      exports: ["isProvisional", "periodKeyForYoY", "hasYoYHistory", "currentPeriodKey"]
      min_lines: 20
    - path: "test/format.test.ts"
      provides: "de-DE formatting assertions"
    - path: "test/period.test.ts"
      provides: "provisional/MoM/YoY-history assertions"
    - path: "test/marts.test.ts"
      provides: "RED stub: P&L formula, sublet net, exclusions, budget grains, months-of-reserve"
    - path: "test/rules-db.test.ts"
      provides: "RED stub: DB-rule ordering, builtin fallback, rule_id resolution"
    - path: "test/reapply.test.ts"
      provides: "RED stub: idempotent re-apply core"
    - path: "test/actions.test.ts"
      provides: "RED stub: zod Server-Action input validators"
  key_links:
    - from: "src/lib/period.ts"
      to: "connection-status.ts"
      via: "mirror the injected-now pure-derivation shape (deriveFreshness)"
      pattern: "now: Date"
---

<objective>
Lay the thin SHARED foundation every later Phase-2 slice reads: the locked de-DE number formatters (`formatEUR`/`formatPct`) and the period/comparability pure helpers (`isProvisional`/`periodKeyForYoY`/`hasYoYHistory`), plus the six Wave-0 test scaffolds from 02-VALIDATION.md so every downstream task has an automated verify or a Wave-0 dependency (Nyquist Dimension 8).

This is NOT a feature slice — it is the foundation slice the MVP nuance in CONTEXT mandates ("format helpers … are a SHARED foundation … build them as a thin foundation slice FIRST"). It is fully autonomous (no DB, no live services): pure TS + Vitest.

Purpose: One source of truth for money/percent formatting (BI-05) and comparability (BI-04) so no later page hand-rolls `Intl` or month math; and a complete RED test harness so the rules/marts/actions plans land against failing tests.
Output: `src/lib/format.ts`, `src/lib/period.ts`, and six `test/*.test.ts` files (two GREEN, four RED stubs).
</objective>

<execution_context>
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/workflows/execute-plan.md
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-bi-house-as-business/02-UI-SPEC.md
@.planning/phases/02-core-bi-house-as-business/02-VALIDATION.md
@.planning/phases/02-core-bi-house-as-business/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Central de-DE money/percent formatters (formatEUR / formatPct)</name>
  <files>src/lib/format.ts, test/format.test.ts</files>
  <read_first>
    - src/lib/utils.ts (the single-source shared-util convention: one tiny file, named exports — PATTERNS analog, utils.ts:1-11)
    - .planning/phases/02-core-bi-house-as-business/02-UI-SPEC.md §Charting "Number formatting — single source of truth" (the LOCKED spec strings)
    - test/rules.test.ts (the Vitest describe/it/expect style used across the repo)
  </read_first>
  <behavior>
    - formatEUR(5038) → "€5.038,00"; formatEUR(820.5) → "€820,50"; formatEUR(100000) → "€100.000,00"
    - formatEUR(42180, 0) → "€42.180" (0 decimals on hero KPI values)
    - formatEUR(-42.18) → "-€42,18" (leading minus, NOT parentheses)
    - formatPct(12.4) → "12,4 %"; formatPct(0) → "0 %" (one decimal max, German U+00A0 space before %)
  </behavior>
  <action>
    Create `src/lib/format.ts` exporting exactly two named functions per the UI-SPEC §Charting LOCKED spec.
    `formatEUR(n: number, decimals?: number): string` = `'€'` prefix + `new Intl.NumberFormat('de-DE', { minimumFractionDigits: decimals ?? 2, maximumFractionDigits: decimals ?? 2 }).format(n)`. The negative sign must lead the whole token (handle by formatting the absolute value then prefixing `-€` when `n < 0`, since `Intl` would place the minus before `€` otherwise — verify the exact output in the test).
    `formatPct(n: number): string` = de-DE one-decimal number followed by a non-breaking space (U+00A0) then `%` (German `12,4 %` convention). Use `Intl.NumberFormat('de-DE', { minimumFractionDigits: 1, maximumFractionDigits: 1 })` on the numeric part.
    Do NOT call `Intl` ad-hoc anywhere else in the codebase — this file is the single source (UI-SPEC). All later numeric cells/axes consume these.
    Write `test/format.test.ts` asserting every case in <behavior> with exact string equality, including the U+00A0 space in the percent output (use ` ` in the expected string).
  </action>
  <verify>
    <automated>pnpm test -- format</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- format` passes (GREEN) with all <behavior> cases asserted by exact-string equality
    - The percent space is the non-breaking U+00A0 (asserted literally in the test), not a normal space
    - No new `new Intl.NumberFormat` call exists outside `src/lib/format.ts` (grep -rn "new Intl.NumberFormat" src | grep -v "src/lib/format.ts" returns nothing)
  </acceptance_criteria>
  <done>Both formatters render the locked de-DE strings; format.test.ts is GREEN; Intl is centralized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Period / comparability pure helpers (provisional, YoY history, period_key math)</name>
  <files>src/lib/period.ts, test/period.test.ts</files>
  <read_first>
    - src/lib/status/connection-status.ts lines 31-44 (the pure derive* helpers with an INJECTED `now: Date` — the exact shape to mirror; PATTERNS analog "Pure-helper + injected-now discipline")
    - src/lib/db/schema.ts lines 250-258 (dim_calendar.period_key = YYYYMM int; MoM compares adjacent period_keys, YoY uses period_key - 100)
    - .planning/phases/02-core-bi-house-as-business/02-UI-SPEC.md §7 (Provisional pill, "Insufficient history" until ~12 months)
  </read_first>
  <behavior>
    - currentPeriodKey(new Date('2026-06-15')) → 202606
    - isProvisional(202606, new Date('2026-06-15')) → true; isProvisional(202605, same now) → false; isProvisional(202607, same now) → false
    - periodKeyForYoY(202606) → 202506; periodKeyForYoY(202601) → 202501
    - hasYoYHistory(list of 11 distinct populated period_keys) → false; hasYoYHistory(list of 12) → true
  </behavior>
  <action>
    Create `src/lib/period.ts` exporting pure functions that all take an injected `now: Date` (mirror `connection-status.ts` deriveFreshness so `test/period.test.ts` is deterministic and DB-free):
    `currentPeriodKey(now: Date): number` → YYYYMM int from now's year/month (use date-fns or `getFullYear()*100 + getMonth()+1`).
    `isProvisional(periodKey: number, now: Date): boolean` → true only when `periodKey === currentPeriodKey(now)` (the current open month; UI-SPEC §7 provisional flag).
    `periodKeyForYoY(periodKey: number): number` → `periodKey - 100` (BI-04 YoY join key).
    `hasYoYHistory(populatedPeriodKeys: number[]): boolean` → true once `>= 12` distinct populated periods exist (the "~12 months" gate; under that, callers show "Not enough history yet" and fall back to MoM).
    Keep all functions pure (no Date.now(), no DB) so the test injects `now`.
    Write `test/period.test.ts` asserting every <behavior> case.
  </action>
  <verify>
    <automated>pnpm test -- period</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- period` passes (GREEN) with all <behavior> cases
    - No function reads `Date.now()` or a DB — `now` is injected (grep "Date.now()" src/lib/period.ts returns nothing)
    - periodKeyForYoY is exactly `periodKey - 100` (crosses the year boundary correctly: 202601 → 202501)
  </acceptance_criteria>
  <done>Provisional/YoY/period helpers are pure + injected-now; period.test.ts is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 RED test stubs for the marts / DB-rules / reapply / actions modules</name>
  <files>test/marts.test.ts, test/rules-db.test.ts, test/reapply.test.ts, test/actions.test.ts</files>
  <read_first>
    - test/rules.test.ts (the FROZEN engine contract + the injected-fake style; mirror its describe/it structure — do NOT modify this file)
    - .planning/phases/02-core-bi-house-as-business/02-VALIDATION.md (the per-requirement test map: which behavior each stub must eventually assert)
    - .planning/phases/02-core-bi-house-as-business/02-RESEARCH.md §Validation Architecture "Wave 0 Gaps" + Pattern 1/2/3/4 (P&L formula, sublet net, exclusions, budget grains) and Pattern 5/7 (DB-rule ordering, rule_id resolution, idempotent reapply)
  </read_first>
  <action>
    Create four Wave-0 test files as RED stubs. Each imports the (not-yet-existing) module under test and asserts the documented behavior so the file FAILS until the corresponding Phase-2 plan lands. Use the mart-test-harness decision: a PURE-TS formula/filter mirror (the SQL views replicate it) — pick the pure-TS mirror over pg-mem to keep tests DB-free (RESEARCH Validation Architecture; lighter, deterministic). Where a stub needs a function that does not exist yet, import it from the planned path and let the import/assertion fail RED (this is the intended Nyquist anchor, not a bug).

    `test/marts.test.ts` (CAT-06, BI-01/02/07): assert the pure P&L formula mirror `result = revenue − investimento − costs + sublet_net`; `margin = result / revenue` with `revenue=0 → margin null/0`; the exclusion invariant (a transferencia or investimento row never enters the cost or revenue SUM); the sublet-net invariant (household revenue excludes sublet gross legs; sublet_net counted exactly once); budget-vs-actual at BOTH grains (category_id null = cost-center grain, category_id set = category grain); months-of-reserve = cash ÷ trailing-3-month avg costs. Import the formula helpers from `@/lib/db/marts` (Plan 03 creates them).

    `test/rules-db.test.ts` (CAT-04, D2-04): assert DB rules are evaluated in (priority, version) order first-match-wins on matchCriteria; falling through to the existing builtin ordering when no DB rule matches; and that a classified row's ruleId resolves to a real builtin uuid via `BUILTIN_RULE_IDS`. Import from `@/lib/ingestion/rules/db-rules` and `@/lib/ingestion/rules/builtins` (Plan 02 creates db-rules + the uuid map).

    `test/reapply.test.ts` (CAT-05): assert the pure re-apply core is idempotent — applying a rule to a fixture set of transactions returns an affected-count > 0 on the first pass and exactly 0 on a second pass over the now-updated set (no destructive rewrite). Import the pure core from `@/lib/actions/reapply-rule` (Plan 06 creates it).

    `test/actions.test.ts` (CAT-04/BI-06): assert the zod validators reject bad payloads (non-uuid txId, missing costCenter, non-boolean createRule) and accept a valid one. Import the exported zod schemas from `@/lib/actions/recategorize` and `@/lib/actions/budgets` (Plans 05/06 create them).

    All four must be discovered by vitest (`test/**/*.test.ts`). Confirm they FAIL now (RED) — that is the success condition for this task.
  </action>
  <verify>
    <automated>pnpm test -- marts rules-db reapply actions 2>&1 | grep -Eq "marts|rules-db|reapply|actions" && echo STUBS_DISCOVERED</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under `test/` and are picked up by vitest (they appear in the run output)
    - Each file FAILS RED (its target module does not exist yet) — verified by the run showing failures for marts/rules-db/reapply/actions, NOT "no tests found"
    - test/rules.test.ts is byte-for-byte unchanged (git diff test/rules.test.ts is empty) — the frozen contract is untouched (Pitfall 6)
    - The mart-harness choice is pure-TS formula mirror (no pg-mem dependency added to package.json)
  </acceptance_criteria>
  <done>Four RED stubs anchor every downstream Phase-2 task to an automated verify; the frozen rules contract is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repo | Source committed to a PUBLIC repo (CV-quality) — no PII, no € amounts, no secrets in format/period helpers or test fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | test fixtures (marts/reapply) | mitigate | Use synthetic round numbers only (e.g. 1000, 500); NO real salary/rent/balance figures, no merchant names that are real counterparties, no IBANs — the source-cleanliness guard (test/source-cleanliness.test.ts) must stay green |
| T-02-02 | Tampering | format.ts numeric output | accept | Pure formatting of caller-supplied numbers; no untrusted parsing, no eval; low risk |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO packages (pure TS + existing Vitest). If a stub is tempted to add pg-mem, do NOT — use the pure-TS mirror per RESEARCH |
</threat_model>

<verification>
- `pnpm test -- format` GREEN; `pnpm test -- period` GREEN.
- `pnpm test` runs all suites; the four new stubs are discovered and FAIL RED (expected); the existing Phase-1 suites (rules, normalize, dedupe, jwt, etc.) stay GREEN.
- `pnpm test:rls` still GREEN (no schema change in this plan).
- `git diff test/rules.test.ts` is empty (frozen contract untouched).
- No `new Intl.NumberFormat` outside `src/lib/format.ts`.
</verification>

<success_criteria>
formatEUR/formatPct render the locked de-DE strings and are the single Intl source (BI-05); the period helpers gate provisional/YoY comparability deterministically (BI-04); all six Wave-0 test files exist with two GREEN and four RED, anchoring every later Phase-2 task to an automated check.
</success_criteria>

<output>
Create `.planning/phases/02-core-bi-house-as-business/02-01-SUMMARY.md` when done.
</output>
