---
phase: 02-core-bi-house-as-business
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/ingestion/rules/builtins.ts
  - src/lib/ingestion/rules/engine.ts
  - src/lib/ingestion/rules/db-rules.ts
  - src/lib/db/schema.ts
  - drizzle/0005_builtin_rules_seed.sql
  - drizzle/0006_budgets_category_id.sql
  - scripts/ingest.ts
  - test/rules-db.test.ts
autonomous: false
requirements: [CAT-04, CAT-06, BI-02]
user_setup: []
must_haves:
  truths:
    - "The engine consults DB rules first (priority, version order, first-match-wins) and falls through to the existing builtin ordering when none match"
    - "A freshly classified transaction stamps a real rule_id uuid (never NULL) that resolves to a rules row"
    - "The engine's emitted cost_center codes are a subset of cost_centers.code (no shared vs compartilhado FK drift)"
    - "budgets has a nullable category_id FK to categories.id, enabling cost-center-grain AND category-grain budgets"
    - "The existing test/rules.test.ts contract stays green (default dbRules = [])"
    - "The 0005 + 0006 migrations are applied to the LIVE database"
  artifacts:
    - path: "drizzle/0005_builtin_rules_seed.sql"
      provides: "6 builtin rules seeded as real rows with deterministic literal uuids"
      contains: "insert into"
    - path: "drizzle/0006_budgets_category_id.sql"
      provides: "ALTER TABLE budgets ADD COLUMN category_id uuid NULL REFERENCES categories(id)"
      contains: "category_id"
    - path: "src/lib/ingestion/rules/db-rules.ts"
      provides: "load + pure-order DB rules; DbRule interface"
      exports: ["DbRule", "orderDbRules"]
    - path: "src/lib/ingestion/rules/builtins.ts"
      provides: "BUILTIN_RULE_IDS uuid map + calibrated sublet signals"
      contains: "BUILTIN_RULE_IDS"
  key_links:
    - from: "scripts/ingest.ts"
      to: "src/lib/ingestion/rules/builtins.ts"
      via: "stamp BUILTIN_RULE_IDS[cls.ruleId] instead of ${null} for rule_id"
      pattern: "BUILTIN_RULE_IDS"
    - from: "src/lib/ingestion/rules/engine.ts"
      to: "src/lib/ingestion/rules/db-rules.ts"
      via: "applyRules accepts dbRules: DbRule[] = [] and evaluates them first"
      pattern: "dbRules"
---

<objective>
Make the rules engine DB-backed and auditable — the foundation every later recategorize/breakdown slice depends on (CONTEXT MVP nuance: "DB-backed rules engine … is a SHARED foundation"). Resolve the three landmines from the downstream brief in one coherent data-layer change:
(1) cost-center code drift — engine emits `shared` (engine.ts:97) but the seed stores `compartilhado` (0003_ingestion.sql:38-47); (2) `rule_id` written as `${null}` at scripts/ingest.ts:196 while the engine returns it (D2-04); (3) `builtins.ts` RuleId string-union → a deterministic builtin→uuid seed.
Also add the nullable `budgets.category_id` (D2-14) so budgeted-vs-actual works at both grains, and apply both migrations to the LIVE DB.

Purpose: CAT-04 (the engine must consult DB rules so a manual override persists forward), CAT-06 (exclusions stay intact — the contract is preserved, not rewritten), BI-02 (category-grain budgets need the new column). Without the live push, TS types come from Drizzle config not the live DB, so the build would falsely pass.
Output: a calibrated DB-backed engine, the builtin-uuid seed, the budgets.category_id migration, the fixed writer, and a live-applied schema.
</objective>

<execution_context>
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/workflows/execute-plan.md
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-bi-house-as-business/02-RESEARCH.md
@.planning/phases/02-core-bi-house-as-business/02-PATTERNS.md
@.planning/phases/02-core-bi-house-as-business/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve cost-center code drift + seed builtin rules with deterministic uuids + budgets.category_id</name>
  <files>src/lib/ingestion/rules/builtins.ts, src/lib/ingestion/rules/engine.ts, src/lib/db/schema.ts, drizzle/0005_builtin_rules_seed.sql, drizzle/0006_budgets_category_id.sql, test/rules-db.test.ts</files>
  <read_first>
    - src/lib/ingestion/rules/engine.ts lines 92-185 (applyRules; the default costCenter `"shared"` at line 97 — the drift source)
    - src/lib/ingestion/rules/builtins.ts lines 16-66 (the 6-member RuleId string union; RULESET_VERSION=1; SUBLET_SIGNALS placeholder; SUBLET_COST_CENTER="sublocacao")
    - drizzle/0003_ingestion.sql lines 35-47 (cost_centers seeded lorenzo/fernanda/compartilhado/sublocacao; the one-time shared→compartilhado UPDATE that did NOT change the engine default)
    - drizzle/0002_seed.sql lines 27-45 (the fixed-uuid `on conflict (id) do nothing` seed convention — PATTERNS analog for 0005)
    - src/lib/db/schema.ts lines 142-158 (rules columns) + 190-196 (budgets columns — currently NO category_id)
    - test/rules.test.ts (the FROZEN 11-assertion contract — must stay green)
  </read_first>
  <behavior>
    - applyRules output for every fixture emits a cost_center that exists in cost_centers.code (lorenzo | fernanda | compartilhado | sublocacao) — never the orphan "shared"
    - BUILTIN_RULE_IDS maps each of the 6 RuleId strings to a fixed literal uuid; those exact uuids exist as rows in the rules table after 0005
    - test/rules.test.ts still passes unchanged (the reconciliation must not break the frozen assertions — if the frozen test expects "shared", reconcile by aliasing the seed, not by editing the test)
  </behavior>
  <action>
    Reconcile the cost-center drift (RESEARCH Pitfall 1 / Runtime State). Decision: ADD a `shared` alias row OR map `shared→compartilhado` in the engine default. Pick the option that keeps test/rules.test.ts green WITHOUT editing its assertions: if the frozen test asserts `"shared"`, change `engine.ts:97` default from the bare string to the seeded code the DB has (`compartilhado`) ONLY IF the test also expects compartilhado; otherwise add a `('shared','Shared')` alias row to cost_centers in 0005 so the engine's `shared` FK-resolves. Read test/rules.test.ts first to determine which path keeps it green, then implement that path. Document the chosen path in the SUMMARY. Either way, after this task the set of cost_center codes the engine can emit MUST be a subset of cost_centers.code.

    Seed the 6 builtin rules as real `rules` rows (D2-04, RESEARCH Pattern 5). Create `drizzle/0005_builtin_rules_seed.sql` following the 0002 fixed-uuid `on conflict (id) do nothing` convention: insert one row per RuleId (`investimento`, `transferencia`, `revenue`, `sublocacao_revenue`, `sublocacao_cost`, `cost_default`) with deterministic literal uuids (e.g. `66666666-6666-6666-6666-666666660001` … `0006`), `version = 1` (RULESET_VERSION), `priority` ordered to mirror the engine's first-match order, and the matching `set_flow_type`/`set_cost_center` where the builtin defines them. Add `export const BUILTIN_RULE_IDS: Record<RuleId, string>` in builtins.ts mirroring those exact literal uuids. Calibrate SUBLET_SIGNALS only if real tenant/utility tokens are known and PII-safe; otherwise keep the placeholder tokens (D2-05 calibration happens against live data later — do NOT commit real tenant names to the public repo).

    Add the nullable `budgets.category_id` (D2-14, RESEARCH "Adding the nullable budgets.category_id"). Extend `budgets` in schema.ts with `categoryId: uuid('category_id').references(() => categories.id)` (nullable = cost-center grain). Hand-write `drizzle/0006_budgets_category_id.sql` = `ALTER TABLE budgets ADD COLUMN category_id uuid REFERENCES categories(id);` (matches the 0003 ADD COLUMN + FK convention). Keep numbering after the existing 0000-0004.

    Update `test/rules-db.test.ts` (the Plan-01 RED stub) to assert BUILTIN_RULE_IDS resolves each RuleId to its seeded uuid and that emitted cost-center codes ⊆ the known cost_centers.code set.
  </action>
  <verify>
    <automated>pnpm test -- rules rules-db</automated>
  </verify>
  <acceptance_criteria>
    - test/rules.test.ts passes unchanged (git diff shows no edits to its assertions)
    - test/rules-db.test.ts asserts BUILTIN_RULE_IDS maps all 6 RuleIds to the literal uuids and emitted codes ⊆ cost_centers.code; it now passes the uuid-map + drift portions
    - `drizzle/0005_builtin_rules_seed.sql` inserts 6 rules rows with `on conflict (id) do nothing`; `drizzle/0006_budgets_category_id.sql` adds the nullable category_id FK
    - schema.ts budgets has `categoryId` referencing categories.id; no € amounts or PII in either SQL file (source-cleanliness guard green)
  </acceptance_criteria>
  <done>Cost-center drift resolved, builtins seeded with fixed uuids, budgets.category_id added; frozen contract green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DB-backed engine (optional dbRules arg) + stamp real rule_id in the writer</name>
  <files>src/lib/ingestion/rules/db-rules.ts, src/lib/ingestion/rules/engine.ts, scripts/ingest.ts, test/rules-db.test.ts</files>
  <read_first>
    - src/lib/ingestion/rules/engine.ts lines 92-185 (applyRules signature + the ordered builtin cascade to preserve)
    - src/lib/status/connection-status.ts lines 31-44, 60-81 (typed read + pure-derive split — the analog for db-rules.ts ordering as pure exported helpers)
    - scripts/ingest.ts lines 105-127 (IngestWriter interface — add getDbRules mirroring getAccounts at 162-180), line 192-199 (the INSERT with rule_id `${null}` at ~196), line 372 (the applyRules call site), line 386 (cls.ruleId already propagated into TxUpsert.ruleId)
    - .planning/phases/02-core-bi-house-as-business/02-RESEARCH.md Pattern 5 (DbRule interface + the "keep the engine pure — pass dbRules as an argument" rule; Pitfall 5/6)
  </read_first>
  <behavior>
    - applyRules(tx, accounts) with no dbRules arg behaves identically to today (frozen test green)
    - applyRules(tx, accounts, dbRules) evaluates dbRules in (priority, version) ascending order, first-match-wins on matchCriteria, before the builtin cascade
    - After an ingest, every classified transaction row has a non-NULL rule_id resolving to a rules.id
  </behavior>
  <action>
    Create `src/lib/ingestion/rules/db-rules.ts` (RESEARCH Pattern 5): export the `DbRule` interface (`id, priority, version, matchCriteria, setCategory, setCostCenter, setFlowType`) and a PURE `orderDbRules(rows: DbRule[]): DbRule[]` that sorts by (priority, version) and a pure `matchDbRule(tx, orderedRules): DbRule | null` (first-match-wins on matchCriteria) — mirror the pure-derive style of connection-status.ts so test/rules-db.test.ts runs DB-free. The DB LOAD (querying the rules table) belongs in the cron WRITE plane, NOT in the engine.

    Extend `applyRules` in engine.ts to accept a third optional argument `dbRules: DbRule[] = []` (Pattern 5; the default keeps test/rules.test.ts green — Pitfall 6). At the top of applyRules, evaluate dbRules via the pure matcher; on a match, return a Classification built from the DB rule (its setCategory/setCostCenter/setFlowType, ruleId = the DB rule id), else fall through UNCHANGED to the existing builtin cascade. Do NOT make the engine query the DB — it stays pure.

    Fix the writer (D2-04 / Pitfall 5). In scripts/ingest.ts: add `getDbRules(): Promise<DbRule[]>` to the `IngestWriter` interface and implement it in `createServiceWriter` (postgres driver, mirror getAccounts at 162-180 — `select id, priority, version, match_criteria, set_category, set_cost_center, set_flow_type from rules`). Pass the loaded rules into `applyRules(toRuleTx(n), accountsById, dbRules)` at the call site (~line 372). Change the INSERT's `rule_id` value from `${null}` (~line 196) to stamp the real uuid: `${BUILTIN_RULE_IDS[t.ruleId] ?? t.ruleId}` — when the classification came from a builtin, map its RuleId string to the seeded uuid; when it came from a DB rule, `cls.ruleId` is already the DB uuid (propagated at line 386 into TxUpsert.ruleId, typed string at line 68). Import BUILTIN_RULE_IDS from builtins.ts.

    Finish `test/rules-db.test.ts`: assert DB-rule ordering + first-match + builtin fallback (no dbRules → builtin path) + rule_id resolution.
  </action>
  <verify>
    <automated>pnpm test -- rules rules-db && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - test/rules.test.ts passes with NO edits (default dbRules=[] preserves behavior)
    - test/rules-db.test.ts is fully GREEN (ordering, first-match, builtin fallback, rule_id resolution)
    - scripts/ingest.ts no longer contains `rule_id` inserted as null — grep -n "rule_id" scripts/ingest.ts shows the BUILTIN_RULE_IDS stamp, and `${null}` no longer sits on the rule_id line
    - engine.ts applyRules signature has the third `dbRules: DbRule[] = []` parameter; the engine imports nothing from `postgres`/Drizzle/`@supabase` (stays pure, server-plane)
    - `pnpm lint` passes
  </acceptance_criteria>
  <done>Engine consults DB rules first then builtins; the writer stamps real rule_ids; both rule suites green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 [BLOCKING]: Apply 0005 + 0006 migrations to the LIVE database</name>
  <files>drizzle/0005_builtin_rules_seed.sql, drizzle/0006_budgets_category_id.sql</files>
  <what-built>
    Two new hand-written migrations are staged: `drizzle/0005_builtin_rules_seed.sql` (6 builtin rules with deterministic uuids + any cost-center alias row) and `drizzle/0006_budgets_category_id.sql` (nullable budgets.category_id FK). These MUST be applied to the live Supabase Postgres, otherwise TS types come from the Drizzle config (not the live DB) and build/verify falsely pass while the live schema lacks the column and the seeded rules — a false-positive verification.
  </what-built>
  <action>
    BLOCKING human-action — applies the staged DB migrations to the LIVE Supabase Postgres (needs the live DATABASE_URL, which is never committed; that is why this plan is autonomous: false).
    1. Load the live DB URL into the environment: `set -a; . ./.env.local; set +a` (DATABASE_URL is the WRITE-plane connection; never committed).
    2. Apply the migrations via the project pattern (numbered hand-written SQL applied by drizzle-kit): `pnpm db:migrate`. If the hand-written 0005/0006 are not in the drizzle journal, apply them explicitly with the postgres driver / the project's migrate path so they run in order after 0004.
    3. Verify live: the rules table contains the 6 seeded uuids (`select count(*) from rules where id like '66666666-%';` → 6); the budgets table has the column (`select column_name from information_schema.columns where table_name='budgets' and column_name='category_id';` → one row); RLS still enabled on rules + budgets.
    4. Regenerate any stale generated types if the project uses `src/lib/database.types.ts` (RESEARCH Runtime State: types go stale after the migration).
    5. Run `pnpm test:rls` to confirm the allowlist guard is still green after the schema change.
  </action>
  <how-to-verify>
    Same steps as <action>: load DATABASE_URL, apply via `pnpm db:migrate`, then confirm 6 seeded rule uuids + budgets.category_id exist live and `pnpm test:rls` is green.
  </how-to-verify>
  <verify>
    <human-check>6 rules rows with id like '66666666-%' exist live; budgets.category_id column exists live; RLS still enabled on rules + budgets; `pnpm test:rls` green.</human-check>
  </verify>
  <done>0005 + 0006 are applied to the live DB (6 seeded builtin-rule uuids + budgets.category_id present), RLS intact, `pnpm test:rls` green.</done>
  <resume-signal>Type "applied" once 0005 + 0006 are live (6 seeded rules + budgets.category_id present) and `pnpm test:rls` is green, or describe the migration error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron (WRITE plane) → Postgres | scripts/ingest.ts uses the postgres driver + DATABASE_URL; bypasses RLS by design (service-equivalent) — server-only, never in the request path |
| developer → public repo | migrations + builtins committed publicly — no PII, no real tenant/merchant names, no € amounts, no DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | drizzle/0005 sublet calibration | mitigate | Do NOT commit real tenant names/IBANs as match_criteria; keep placeholder tokens in the public seed, calibrate against live data via the in-app rule editor later (D2-05). source-cleanliness guard must stay green |
| T-02-04 | Tampering | DB-rule matching | mitigate | matchCriteria comparison is a parameterized read in the cron; the engine never string-concats user input into SQL; DbRule load uses the postgres tagged-template driver |
| T-02-05 | Elevation of Privilege | budgets.category_id new column | mitigate | The new column inherits the existing `allowlist_all for all to authenticated` RLS on budgets (0001) — verify RLS still enabled post-ALTER; no new policy gap |
| T-02-06 | Information Disclosure | engine purity | mitigate | engine.ts must NOT import postgres/Drizzle/service_role — DB rules are passed in as an argument; keeps the Phase-0 bundle-grep + ESLint guard green |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO new packages; the legitimacy audit in 02-RESEARCH.md confirms zero new installs |
</threat_model>

<verification>
- `pnpm test -- rules` GREEN (frozen contract unchanged); `pnpm test -- rules-db` GREEN.
- `pnpm lint` GREEN; engine.ts imports nothing from postgres/Drizzle/@supabase.
- After the [BLOCKING] push: live rules table has 6 seeded uuid rows; budgets has category_id; `pnpm test:rls` GREEN.
- A fresh ingest stamps a non-NULL rule_id on every row (`select count(*) from transactions where rule_id is null` after the next cron run → 0 for newly classified rows).
- No `${null}` on the rule_id INSERT line in scripts/ingest.ts.
</verification>

<success_criteria>
The engine is DB-backed (consults DB rules first, falls back to builtins, frozen test green), every classification is auditable (real rule_id uuid, never NULL), the shared/compartilhado drift is gone (emitted codes ⊆ cost_centers.code), budgets supports category-grain (nullable category_id), and both migrations are LIVE.
</success_criteria>

<output>
Create `.planning/phases/02-core-bi-house-as-business/02-02-SUMMARY.md` when done.
</output>
