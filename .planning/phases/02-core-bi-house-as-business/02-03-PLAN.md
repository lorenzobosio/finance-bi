---
phase: 02-core-bi-house-as-business
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - drizzle/0007_marts.sql
  - drizzle/0008_marts_rls.sql
  - src/lib/db/marts.ts
  - src/lib/db/schema.ts
  - scripts/ingest.ts
  - test/marts.test.ts
autonomous: false
requirements: [BI-01, BI-02, BI-03, BI-04, BI-07, CAT-06]
user_setup: []
must_haves:
  truths:
    - "v_pnl_monthly returns one row per period_key on the dim_calendar spine — empty months render €0, never missing"
    - "Household revenue/cost SUMs exclude investimento, transferencia, AND the sublocacao legs; sublet_net is counted exactly once"
    - "result = revenue − investimento − costs + sublet_net; margin = result / nullif(revenue,0)"
    - "Budget-vs-actual joins at both grains (category_id null = cost-center grain, category_id set = category grain); absent budget rows are NOT synthesized as €0 caps"
    - "Breakdown marts render an Uncategorized bucket (category_id IS NULL → coalesce label) — never drop rows"
    - "v_balance_trend yields net worth + months-of-reserve (cash ÷ trailing-3-month avg costs); balances has UNIQUE(account_id, as_of_date)"
    - "Every new view/table ships with RLS enabled + the allowlist policy"
    - "0007 + 0008 are applied to the LIVE database"
  artifacts:
    - path: "drizzle/0007_marts.sql"
      provides: "v_pnl_monthly, v_sublet_pnl, v_costcenter_bva, v_category_breakdown, v_pct_of_revenue, v_balance_trend, v_home_kpis"
      contains: "create view"
    - path: "drizzle/0008_marts_rls.sql"
      provides: "RLS on every new view/table + balances UNIQUE(account_id, as_of_date)"
      contains: "row level security"
    - path: "src/lib/db/marts.ts"
      provides: "typed pgView(...).existing() handles + pure formula mirror consumed by test/marts.test.ts"
      exports: ["vPnlMonthly", "vSubletPnl", "vCostcenterBva", "vCategoryBreakdown", "vPctOfRevenue", "vBalanceTrend", "vHomeKpis", "computePnl", "computeMonthsOfReserve"]
  key_links:
    - from: "drizzle/0007_marts.sql"
      to: "src/lib/db/schema.ts (dim_calendar, transactions, budgets, balances)"
      via: "LEFT JOIN facts onto the dense dim_calendar period_key spine"
      pattern: "dim_calendar"
    - from: "src/lib/db/marts.ts"
      to: "drizzle/0007_marts.sql"
      via: "pgView(name).existing() typed handles for RSC reads"
      pattern: "existing\\(\\)"
---

<objective>
Build the calendar-joined SQL marts that power every Phase-2 dashboard — the SHARED data foundation (CONTEXT MVP nuance: "the SQL marts … are a SHARED foundation … build them as a thin foundation slice FIRST"). One queryable source of truth keeps the math out of TS and DRY across the 5 pages. This is CONTEXT's intended slice 2.1's data layer.

Resolve the correctness keystones in SQL: exclusion discipline (CAT-06 / D2-11 — investimento + transferencia never in cost/revenue), the Sublocação net roll-up exactly once (D2-06/07/08), budget-vs-actual at two grains (D2-14), the %-of-revenue analysis (D2-15), Uncategorized graceful-degrade (D2-01), and the balance/net-worth/months-of-reserve trend (BI-07). Every mart drives off the dense dim_calendar spine so empty months are €0 rows (BI-04). Ships RLS on every new object and the balances UNIQUE index; applies both migrations LIVE.

Purpose: BI-01 (P&L + margin), BI-02 (budget vs actual), BI-03 (breakdowns + %-of-revenue), BI-04 (MoM-comparable zero-fill), BI-07 (balance trend / months-of-reserve), CAT-06 (exclusions preserved). Without the live push, build/types pass against Drizzle config while the live DB lacks the views — a false-positive verification.
Output: 7 SQL views, marts RLS + balances unique idx, typed pgView handles + a pure formula mirror, calibrated balance capture, all LIVE.
</objective>

<execution_context>
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/workflows/execute-plan.md
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-bi-house-as-business/02-RESEARCH.md
@.planning/phases/02-core-bi-house-as-business/02-PATTERNS.md
@.planning/phases/02-core-bi-house-as-business/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: P&L / sublet-net / budget-vs-actual / breakdown / %-of-revenue marts (pure mirror + SQL)</name>
  <files>drizzle/0007_marts.sql, src/lib/db/marts.ts, test/marts.test.ts</files>
  <read_first>
    - .planning/phases/02-core-bi-house-as-business/02-RESEARCH.md Pattern 1 (calendar-spine zero-fill), Pattern 2 (exclusion discipline), Pattern 3 (sublet net roll-up — the locked formula `revenue − investimento − costs + sublet_net`), Pattern 4 (budget two-grain join), Pattern 9 (Uncategorized coalesce); the "Defining a mart as a typed Drizzle view" code example
    - drizzle/0001_rls_policies.sql lines 88-97 (the hand-written DDL + per-object RLS block convention)
    - drizzle/0002_seed.sql (the category taxonomy already seeded: Housing/Groceries/… — reconcile breakdown labels against the real seed, do NOT relabel)
    - src/lib/db/schema.ts lines 159-196 (transactions: flow_type, cost_center, category_id, booking_date, amount_eur signed; budgets: cost_center, category_id (added in Plan 02), period_key) + 250-258 (dim_calendar.period_key)
    - .planning/phases/02-core-bi-house-as-business/02-UI-SPEC.md §2 (waterfall step order) + §Conventions (margin = result ÷ revenue "% of net revenue")
  </read_first>
  <behavior>
    - computePnl over a fixture: revenue sums only flow_type='revenue' AND cost_center<>'sublocacao'; costs sums only flow_type='cost' AND cost_center<>'sublocacao'; investimento is its own line; result = revenue − investimento − costs + sublet_net; a transferencia row contributes to NONE of revenue/costs/investimento
    - sublet_net is signed and counted exactly once; household revenue excludes sublet gross legs (a sublet rent-received row does NOT inflate household revenue)
    - budget-vs-actual: a row with category_id NULL matches actuals at cost-center grain; a row with category_id set matches that category; absent budgets produce no synthetic €0 cap
    - a category-less (category_id IS NULL) cost row appears under an "Uncategorized" bucket, not dropped
  </behavior>
  <action>
    Create `drizzle/0007_marts.sql` as hand-written `create view` DDL (matches the 0001 hand-written-SQL convention; Drizzle generates schema DDL, views are hand-written). Define each view driven by `dim_calendar` LEFT JOIN on period_key (Pattern 1 zero-fill):
    - `v_pnl_monthly`: per period_key — revenue (`sum(amount_eur) filter (where flow_type='revenue' and cost_center<>'sublocacao')`), costs (`sum(-amount_eur) filter (where flow_type='cost' and cost_center<>'sublocacao')`), investimento (`sum(-amount_eur) filter (where flow_type='investimento')`), the sublet_net pulled from v_sublet_pnl, result = revenue − investimento − costs + sublet_net, margin = result / nullif(revenue,0). coalesce all SUMs to 0 (BI-04 empty=€0). NEVER include transferencia in any bucket (Pattern 2).
    - `v_sublet_pnl` (D2-06/07/08): the ONLY place sublet gross legs appear — sublet_revenue (`filter where flow_type='revenue'`), sublet_costs (`filter where flow_type='cost'`), sublet_net (signed sum) WHERE cost_center='sublocacao', per period_key.
    - `v_costcenter_bva` (BI-02/D2-14): budgets LEFT JOIN actuals at both grains (`(b.category_id is null or t.category_id = b.category_id)` per Pattern 4); do NOT synthesize €0 budget rows (D2-12 — absence means "not set").
    - `v_category_breakdown` (BI-03/D2-01): cost SUM grouped by `coalesce(category_name,'Uncategorized')` × period_key; also expose account and person (cost_center) breakdown grains (Pattern 9 — Uncategorized always present).
    - `v_pct_of_revenue` (D2-15): each category's cost as a share of that period's salary revenue (`category_cost / nullif(revenue,0)`).
    Use parameterized/static SQL only — no user input concatenated. Reconcile labels against the existing 0002 category seed (RESEARCH A6 — keep the seed, Config edits later; do not commit a relabel).
    Preserve the engine's classification invariants (do NOT re-implement them, just don't break them in SQL): `revenue` = net salary already classified by the engine (D2-09) — the mart sums flow_type='revenue', which is the net amount that landed; the one-off-bonus distinction is carried by `is_recurring` on the row (the mart may expose it so the UI annotates MoM, D2-09). The household result subtracts investimento exactly once and the cost bucket already includes the one-off June "Instant Access Savings" transfer as a normal `cost` (D2-10 — Patrimônio is part of net worth / the cost bucket, NOT a separate goal bucket); do not special-case or re-flag it in the marts.

    Create `src/lib/db/marts.ts`: typed `pgView('v_pnl_monthly').existing()` handles (and the other views) for RSC reads (RESEARCH "Defining a mart as a typed Drizzle view"), PLUS pure exported formula helpers `computePnl(rows)` and the breakdown/budget mirrors that test/marts.test.ts asserts — these mirror the SQL exactly so the math is unit-tested DB-free (the pure-TS-mirror harness chosen in Plan 01 Task 3).

    Finish `test/marts.test.ts`: assert the full <behavior> set against the pure mirror.
  </action>
  <verify>
    <automated>pnpm test -- marts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- marts` GREEN for the P&L formula, exclusion invariant, sublet-net-once invariant, two-grain budget join, and Uncategorized bucket
    - 0007_marts.sql defines v_pnl_monthly, v_sublet_pnl, v_costcenter_bva, v_category_breakdown, v_pct_of_revenue with the dim_calendar LEFT JOIN spine and the `cost_center<>'sublocacao'` exclusion in every household SUM
    - No household SUM includes transferencia or investimento in revenue/costs (grep the SQL — every revenue/cost filter excludes those flow_types)
    - marts.ts exports typed .existing() handles + computePnl; no € amounts or PII in the SQL
  </acceptance_criteria>
  <done>The P&L/sublet/budget/breakdown/%-of-revenue marts exist with the locked formula and exclusions; the pure mirror is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Balance / net-worth / months-of-reserve mart + balances UNIQUE idx + per-object RLS</name>
  <files>drizzle/0007_marts.sql, drizzle/0008_marts_rls.sql, src/lib/db/marts.ts, src/lib/db/schema.ts, scripts/ingest.ts, test/marts.test.ts</files>
  <read_first>
    - .planning/phases/02-core-bi-house-as-business/02-RESEARCH.md Pattern 10 (balance capture already wired via upsertBalance; the balances UNIQUE landmine) + UI-SPEC §Conventions (months-of-reserve = liquid cash ÷ trailing-3-month avg monthly costs)
    - scripts/ingest.ts lines 204-216 (upsertBalance check-then-write idempotency — calibrate, do not rewrite)
    - src/lib/db/schema.ts lines 228-239 (balances has only balances_account_id_idx — NO UNIQUE(account_id, as_of_date))
    - drizzle/0001_rls_policies.sql lines 88-97 (the `enable row level security` + `allowlist_all for all to authenticated` block to copy per new object)
    - .planning/phases/02-core-bi-house-as-business/02-PATTERNS.md "Migration convention" + "Every new view/table ships with RLS — no exceptions (T-00-04)"
  </read_first>
  <behavior>
    - computeMonthsOfReserve(cash, last3MonthsCosts) = cash / (avg of trailing-3-month costs); returns a months number; guards divide-by-zero
    - v_balance_trend yields one net-worth figure per day (latest balance per account, summed) across the dim_calendar spine
    - inserting two balance rows for the same (account_id, as_of_date) is prevented by the UNIQUE index
  </behavior>
  <action>
    Add `v_balance_trend` to `drizzle/0007_marts.sql` (BI-07, Pattern 10): latest balance per account per day → summed net worth, joined to the dim_calendar spine for MoM comparability; expose the inputs months-of-reserve needs (liquid cash position + the P&L costs bucket). Add `computeMonthsOfReserve` to marts.ts as the pure helper (cash ÷ trailing-3-month avg costs per UI-SPEC §Conventions) and assert it in test/marts.test.ts.

    Add the balances UNIQUE index (Pattern 10 landmine): extend schema.ts balances with `unique('balances_account_date_uq').on(t.accountId, t.asOfDate)` and include `create unique index` (or `alter table … add constraint`) in `drizzle/0008_marts_rls.sql`. Confirm upsertBalance's check-then-write (ingest.ts:204-216) is consistent with the new constraint (it already upserts by (account_id, as_of_date) — calibrate only if the constraint name/ON CONFLICT target needs alignment; do not rewrite the idempotency logic).

    Create `drizzle/0008_marts_rls.sql`: for EVERY new view/table from 0007/0008, copy the per-object RLS block from 0001 (`enable row level security` + `create policy allowlist_all for all to authenticated using/with check is_email_allowed(jwt email)`). Views inherit the underlying tables' RLS when defined with `security_invoker=on` — set `security_invoker = on` on the views so RSC reads run under the caller's JWT and the allowlist applies (verify this is the Postgres-15 mechanism; if a view cannot carry a direct policy, security_invoker is the correct control). No new object ships without RLS (T-00-04).
  </action>
  <verify>
    <automated>pnpm test -- marts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- marts` GREEN including computeMonthsOfReserve (with the divide-by-zero guard) and the net-worth aggregation mirror
    - schema.ts balances declares a UNIQUE on (account_id, as_of_date); 0008 contains the matching constraint/index
    - 0008 enables RLS / security_invoker on every new view + table from this plan — grep shows a control for each of the 7 views and the balances change; none is left unprotected
    - No PII / € amounts in 0007 or 0008
  </acceptance_criteria>
  <done>Balance trend + months-of-reserve mart exist, balances is duplicate-safe, every new object is RLS-protected.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 [BLOCKING]: Apply 0007 + 0008 marts + RLS to the LIVE database</name>
  <files>drizzle/0007_marts.sql, drizzle/0008_marts_rls.sql</files>
  <what-built>
    The 7 SQL marts (`drizzle/0007_marts.sql`) and their RLS + the balances UNIQUE index (`drizzle/0008_marts_rls.sql`) are staged. They MUST be created on the live Supabase Postgres — the typed `pgView(...).existing()` handles in marts.ts assume the views exist in the live DB, and every dashboard page (Plans 04/05/06) reads them under RLS. TS types come from the Drizzle config, so build/verify would falsely pass without the live views, and an unprotected view would leak data across the allowlist.
  </what-built>
  <action>
    BLOCKING human-action — creates the staged marts + RLS on the LIVE Supabase Postgres (needs the live DATABASE_URL, never committed; hence autonomous: false).
    1. `set -a; . ./.env.local; set +a` to load DATABASE_URL (WRITE plane; never committed).
    2. Apply the migrations in order after 0006 via the project pattern: `pnpm db:migrate` (or apply 0007 then 0008 explicitly with the postgres driver if they are not yet in the drizzle journal).
    3. Verify the 7 views exist: `select table_name from information_schema.views where table_name like 'v_%';` → returns v_pnl_monthly, v_sublet_pnl, v_costcenter_bva, v_category_breakdown, v_pct_of_revenue, v_balance_trend, v_home_kpis.
    4. Verify RLS/security: each new view runs under the caller (security_invoker=on) and the balances UNIQUE(account_id, as_of_date) constraint exists (`select conname from pg_constraint where conrelid='public.balances'::regclass and contype='u';`).
    5. Spot-check correctness against any existing live data: `select period_key, revenue, costs, investimento, sublet_net, result, margin from v_pnl_monthly where revenue <> 0 order by period_key desc limit 3;` — confirm household revenue excludes sublet legs and result matches `revenue − investimento − costs + sublet_net`.
    6. Run `pnpm test:rls` — the allowlist guard must stay green after the new objects land.
  </action>
  <how-to-verify>
    Same steps as <action>: load DATABASE_URL, apply via `pnpm db:migrate`, then confirm all 7 v_* views exist with security_invoker, the balances UNIQUE constraint exists, the v_pnl_monthly spot-check matches the locked formula, and `pnpm test:rls` is green.
  </how-to-verify>
  <verify>
    <human-check>All 7 v_* views exist live with security_invoker=on (or an explicit allowlist policy); balances UNIQUE(account_id, as_of_date) exists; v_pnl_monthly.result == revenue − investimento − costs + sublet_net on real data; `pnpm test:rls` green.</human-check>
  </verify>
  <done>0007 + 0008 are applied live — 7 RLS-protected marts + the balances UNIQUE constraint exist, the P&L spot-check matches the locked formula, `pnpm test:rls` green.</done>
  <resume-signal>Type "applied" once all 7 views exist live, RLS/security_invoker is set on each, the balances UNIQUE constraint exists, and `pnpm test:rls` is green — or describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC page (READ plane) → mart views | Server Components read v_* under the user JWT via @supabase/ssr; RLS/security_invoker enforces the 2-email allowlist on every view |
| cron (WRITE plane) → balances | upsertBalance via postgres driver bypasses RLS by design; server-only |
| developer → public repo | SQL DDL committed publicly — no € amounts, no real merchant/tenant/account names, no DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | new v_* views | mitigate | Every view is created with `security_invoker = on` (or an explicit allowlist policy) in 0008 so RLS applies to RSC reads; T-00-04 — no object ships without RLS. Verified live in the [BLOCKING] task |
| T-02-08 | Tampering | mart SQL params | mitigate | Views are static DDL; the read path is `supabase.from('v_*').select()` with parameterized `.eq('period_key', …)` filters — no string-concat of user input into SQL (RESEARCH Security Domain) |
| T-02-09 | Tampering | balances duplicate rows | mitigate | UNIQUE(account_id, as_of_date) + upsertBalance check-then-write makes a concurrent cron run idempotent (Pattern 10 landmine closed) |
| T-02-10 | Information Disclosure | committed SQL fixtures | mitigate | No real figures/PII in 0007/0008 or test/marts.test.ts fixtures (synthetic round numbers); source-cleanliness guard green |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; pure-TS mart mirror chosen over pg-mem (no install) |
</threat_model>

<verification>
- `pnpm test -- marts` GREEN (P&L formula, exclusions, sublet-net-once, two-grain budgets, Uncategorized, months-of-reserve).
- After [BLOCKING] push: 7 v_* views exist live with security_invoker/RLS; balances UNIQUE(account_id, as_of_date) exists; `pnpm test:rls` GREEN.
- Live spot-check: v_pnl_monthly.result == revenue − investimento − costs + sublet_net; household revenue excludes sublet gross.
- `pnpm test` full suite GREEN; `pnpm lint` GREEN.
</verification>

<success_criteria>
The calendar-joined marts compute the locked P&L/margin, net the sublet exactly once, support budgets at two grains, surface %-of-revenue and an Uncategorized bucket, and yield net-worth + months-of-reserve — all MoM-comparable (empty=€0), all RLS-protected, all LIVE.
</success_criteria>

<output>
Create `.planning/phases/02-core-bi-house-as-business/02-03-SUMMARY.md` when done.
</output>
