---
phase: 02-core-bi-house-as-business
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/app/(protected)/layout.tsx
  - src/app/(protected)/page.tsx
  - src/components/kpi-card.tsx
  - src/components/charts/progress-bar.tsx
  - src/components/month-selector.tsx
  - src/components/ui/chart.tsx
autonomous: true
requirements: [BI-05, BI-04, BI-01]
user_setup: []
must_haves:
  truths:
    - "A logged-in user sees the 4 headline KPI cards on Home in question order: (1) €100k invested progress, (2) €4k this month, (3) per-person budget status, (4) margin %"
    - "Every page reads the shared ?period=YYYYMM month selector; next is disabled at the current month"
    - "The current open month shows a Provisional pill; the €4k card never shows a red 'Missed' state for the open month"
    - "The Budgets-not-set state renders a distinct neutral 'Budgets not set' (never a false green)"
    - "The freshness banner mounts once in the shell and appears on every dashboard"
    - "All money/percent render via formatEUR/formatPct (de-DE), mono + tabular-nums"
  artifacts:
    - path: "src/app/(protected)/layout.tsx"
      provides: "app shell: sidebar/bottom-nav + StatusBanners + shared month selector"
      min_lines: 40
    - path: "src/app/(protected)/page.tsx"
      provides: "Home — 4 KPI cards reading v_home_kpis/v_pnl_monthly"
      min_lines: 50
    - path: "src/components/kpi-card.tsx"
      provides: "reusable KpiCard (label + big mono value + status/delta + optional mini-viz)"
      exports: ["KpiCard"]
    - path: "src/components/month-selector.tsx"
      provides: "shared ?period=YYYYMM switcher"
      exports: ["MonthSelector"]
  key_links:
    - from: "src/app/(protected)/page.tsx"
      to: "src/lib/db/marts.ts"
      via: "supabase.from('v_home_kpis')/('v_pnl_monthly').select() under RLS for the selected period"
      pattern: "v_home_kpis|v_pnl_monthly"
    - from: "src/app/(protected)/layout.tsx"
      to: "src/components/status/status-banners.tsx"
      via: "mount <StatusBanners /> once full-bleed at the shell top (reuse Phase-1 asset)"
      pattern: "StatusBanners"
---

<objective>
Ship the FIRST user-facing vertical slice: the app shell (nav + shared month selector + the reused freshness banner) and the Home dashboard with the 4 headline KPIs answerable in under a minute (BI-05) — the product's North-Star test. After this plan, a logged-in user can open the app and read "how far to €100k, did we hit €4k, did anyone blow budget, what's the margin" for any selected month, with first-class provisional/empty states (BI-04).

This is CONTEXT's slice 2.2 (Home). It reads the marts from Plan 03 through the existing @supabase/ssr READ plane under RLS — no service_role, no Drizzle client in a page (RESEARCH Pitfall 3).

Purpose: BI-05 (4 headline KPIs, mobile-first), BI-04 (provisional/empty comparability states surfaced), BI-01 (margin KPI from v_pnl_monthly). The shared month selector (?period) is the app-wide state every later page keys off.
Output: the shell layout, the Home page, a reusable KpiCard, the month selector, a ProgressBar chart block, and the shadcn chart scaffold.
</objective>

<execution_context>
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/workflows/execute-plan.md
@/Users/lorenzobrazilbosio/Developer/Claude-code/revolut-bi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-bi-house-as-business/02-UI-SPEC.md
@.planning/phases/02-core-bi-house-as-business/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App shell layout — nav + shared ?period month selector + reused freshness banner</name>
  <files>src/app/(protected)/layout.tsx, src/components/month-selector.tsx, src/components/ui/chart.tsx</files>
  <read_first>
    - src/components/status/status-banners.tsx lines 14-25 (mount <StatusBanners /> once full-bleed at the shell top — async Server Component reading RLS state; reuse VERBATIM, PATTERNS analog)
    - src/app/(protected)/page.tsx lines 1-60 (today's stub Home + the RLS-read RSC pattern)
    - .planning/phases/02-core-bi-house-as-business/02-UI-SPEC.md §0 (App Shell: desktop sidebar ~240px / mobile bottom-nav 56px+safe-area; nav order Home·Gastos·Cost Centers·Transações·Config + disabled Goal placeholder; the MANDATORY shared month selector ?period=YYYYMM, current-month default, next disabled at current month, MMM yyyy label)
    - src/lib/period.ts (currentPeriodKey/isProvisional from Plan 01 — the selector's bounds + provisional flag)
  </read_first>
  <action>
    Scaffold the shadcn chart base once: `npx shadcn@latest add chart` → creates `src/components/ui/chart.tsx` (Recharts-3-native, shadcn PR #8486). Apply the Recharts-3 paste rules (UI-SPEC §Charting): `var(--chart-1)` not `hsl(var(--chart-1))`; ChartContainer carries a min-h. If the CLI is unavailable, copy the official chart source manually (first-party, copy-paste safe).

    Create `src/app/(protected)/layout.tsx` (PATTERNS analog: page.tsx + status-banners): mount `<StatusBanners />` full-bleed at the very top (reuse the Phase-1 component verbatim — ReconnectBanner stacks above FreshnessBanner). Add the desktop sidebar (≥lg, ~240px, --sidebar surface, active item = --primary text + --sidebar-accent fill + left indicator) and the mobile bottom tab bar (<lg, fixed, 56px + env(safe-area-inset-bottom), 5 icon+label tabs). Nav order: Home · Gastos · Cost Centers · Transações · Config, plus a disabled greyed "Goal (Phase 3)" placeholder. Content area: 32px padding desktop, max-w-7xl. Page header pattern: h1 (20px/600) left + the month selector right. Touch targets ≥44px on mobile.

    Create `src/components/month-selector.tsx` (UI-SPEC §0, MANDATORY shared state): a `‹ prev / next ›` switcher whose value lives in the URL search param `?period=YYYYMM` (default = currentPeriodKey(now)); the `next` arrow is disabled when the selected period is the current month (no future months); the label is `MMM yyyy` via date-fns (mono). Every mart-backed page reads this same param so provisional flagging, MoM deltas, and "did we hit €4k this month?" all key off one period. Use it as a client component updating the URL; pages read `searchParams.period` server-side.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5 && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` succeeds and `pnpm lint` passes
    - layout.tsx mounts `<StatusBanners />` once (grep shows the import + single mount) and renders the 5-item nav in the locked order + the disabled Goal placeholder
    - month-selector.tsx writes/reads `?period=YYYYMM`, defaults to the current month, and disables `next` at the current month
    - src/components/ui/chart.tsx exists; no `hsl(var(--chart-` occurrences in any new chart file (Recharts-3 rule)
    - No `service_role`, `DATABASE_URL`, or postgres/Drizzle import anywhere under src/app (Pitfall 3)
  </acceptance_criteria>
  <done>The shell renders nav + the shared month selector + the freshness banner on every protected page.</done>
</task>

<task type="auto">
  <name>Task 2: Home — 4 headline KPI cards from the marts (BI-05) + reusable KpiCard + ProgressBar</name>
  <files>src/app/(protected)/page.tsx, src/components/kpi-card.tsx, src/components/charts/progress-bar.tsx</files>
  <read_first>
    - src/app/(protected)/page.tsx lines 9-56 (the RLS-read pattern: createClient → supabase.from(...).select(); error → role="alert" text-destructive; empty → muted "No …" — REPLACE this stub with the real Home)
    - .planning/phases/02-core-bi-house-as-business/02-UI-SPEC.md §1 (Home 4 KPI cards + the EXACT card states), §7 (first-class states), §Copywriting Contract (the exact KPI copy strings)
    - src/components/ui/button.tsx lines 1-67 (the shadcn cva + cn component convention for KpiCard); src/lib/utils.ts (cn/cx alias for Tremor Raw ProgressBar)
    - src/lib/format.ts + src/lib/period.ts (formatEUR/formatPct + isProvisional — from Plan 01)
    - src/lib/db/marts.ts (v_home_kpis / v_pnl_monthly typed handles — from Plan 03)
  </read_first>
  <action>
    Copy the Tremor Raw ProgressBar block into `src/components/charts/progress-bar.tsx` (it imports `cx`, already aliased in utils.ts — copies unmodified; FND-06). Apply Recharts-3 paste rules. Expose `role="progressbar"` with aria-valuenow/min/max + a visible mono value (UI-SPEC a11y).

    Create `src/components/kpi-card.tsx` (single reusable KpiCard, UI-SPEC §1 anatomy): props for label (14px/600 muted + lucide glyph, aria-hidden), big value (Display 32px/600 mono via formatEUR/formatPct), a status/delta row (delta chip with TrendingUp/Down icon + --gain/--loss, OR a status pill with CircleCheck/TriangleAlert), and an optional mini-viz slot (ProgressBar/sparkline). Color is NEVER the only signal — always icon + text + color.

    Replace `src/app/(protected)/page.tsx` with the real Home (RLS-read pattern from the stub). Read the selected `?period` and query the marts via `supabase.from('v_home_kpis')`/`from('v_pnl_monthly')` under RLS. Render the 4 KPI cards in question order (single column mobile → 2×2 md → 4-across xl; the €100k card gets STRUCTURAL emphasis — col-span-2 desktop / elevated --card+ring — NOT a bigger font):
    1. €100k progress: cumulative investimento cost-basis value + ProgressBar toward €100.000 + "% to goal"; label "Invested (cost basis)"; NO milestones/ETA/streak (Phase 3).
    2. €4k this month: ≥€4.000 → "On track" --gain CircleCheck; <€4.000 AND current open month → "€{remaining} to go" --warning (provisional); <€4.000 AND past closed month → "Missed — €{shortfall} short" --loss. The open month is NEVER red.
    3. Per-person budget: names who — "{name} over budget"/"Both over budget" --loss; all set within cap → "On track"; NO budgets set → distinct "Budgets not set" --neutral-data + "Set budgets →" (never a false green).
    4. Margin %: value = margin "% of net revenue" (formatPct) + delta vs last month; --gain if positive & improving.
    Optional secondary grid below: savings-rate + months-of-reserve (Claude's discretion, in scope). Surface the Provisional amber pill next to the period label for the current month (BI-04). First-use: if no ingested data exists, render the calm "Synchronizing — your first data appears tomorrow" band with €0 states beneath (UI-SPEC §7). Each whole card is tappable → its detail page. Use the exact Copywriting Contract strings.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5 && pnpm lint && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` + `pnpm lint` + `pnpm test` all pass (the Plan-01/02/03 unit suites stay green)
    - page.tsx reads the marts via `supabase.from('v_home_kpis'|'v_pnl_monthly')` (grep) under the @supabase/ssr client — NOT the Drizzle/postgres client (Pitfall 3)
    - The 4 cards render in question order; the €100k card has structural emphasis (col-span/ring) and type stays text-3xl (no text-4xl anywhere — grep returns none)
    - The €4k card has NO red/Missed path reachable for the current open month (the conditional gates on past+closed only)
    - The no-budgets state renders "Budgets not set" (neutral), never "On track"
    - All monetary/percent values flow through formatEUR/formatPct (no ad-hoc Intl)
  </acceptance_criteria>
  <done>Home answers the 4 North-Star questions for any selected month with correct provisional/empty states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RSC page | The page is a Server Component; the browser never queries the DB — data is fetched server-side under the user JWT and handed down as props |
| RSC page (READ plane) → mart views | supabase.from('v_*') runs under the user JWT via @supabase/ssr; RLS/security_invoker enforces the 2-email allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information Disclosure / EoP | Home page DB access | mitigate | Pages use ONLY the @supabase/ssr server client (anon + user JWT); NEVER service_role / DATABASE_URL / Drizzle in src/app — enforced by the Phase-0 ESLint guard + CI bundle-grep (FND-03) |
| T-02-12 | Tampering | ?period search param | mitigate | period is validated/clamped to a YYYYMM int (period.ts helpers); an out-of-range or non-numeric value falls back to the current month — no raw param reaches a query unparsed |
| T-02-13 | Information Disclosure | KPI rendering | accept | All figures are the logged-in couple's own data already gated by RLS; no cross-tenant exposure since both allowlisted users share visibility by design |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No npm installs; `npx shadcn add chart` writes first-party component source only (official radix-nova registry; components.json registries={}) — RESEARCH legitimacy audit confirms zero third-party installs |
</threat_model>

<verification>
- `pnpm build` + `pnpm lint` + `pnpm test` GREEN.
- Manual (UAT): log in, open Home — the 4 KPI cards render in question order; switch months via the selector; the current month shows Provisional; the €4k card never shows red for the open month; no-budgets shows "Budgets not set".
- Grep: no `service_role`/`DATABASE_URL`/Drizzle import under src/app; no `hsl(var(--chart-`; no `text-4xl`.
</verification>

<success_criteria>
A logged-in user reads the 4 North-Star KPIs on Home for any selected month, with the shared ?period selector, the freshness banner, structural €100k emphasis, correct provisional/no-budget states, and de-DE formatting — the first end-to-end user-facing slice of Phase 2.
</success_criteria>

<output>
Create `.planning/phases/02-core-bi-house-as-business/02-04-SUMMARY.md` when done.
</output>
