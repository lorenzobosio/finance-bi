// bills-section — STUB (filled by 09-05, BILL-01). Accepts the page-resolved { demoFilter, asOf }
// props so the /cashflow page mounts it unchanged next wave, and returns null until the bills engine
// + calendar land. No demo-bearing read yet (nothing to thread).

export async function BillsSection(_props: {
  demoFilter: boolean;
  asOf: Date;
}): Promise<null> {
  return null;
}
