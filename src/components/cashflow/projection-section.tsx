// projection-section — STUB (filled by 09-06, PROJ-01). Accepts the page-resolved
// { demoFilter, asOf } props so the /cashflow page mounts it unchanged next wave, and returns null
// until the cash-flow projection engine + chart land. No demo-bearing read yet (nothing to thread).

export async function ProjectionSection(_props: {
  demoFilter: boolean;
  asOf: Date;
}): Promise<null> {
  return null;
}
