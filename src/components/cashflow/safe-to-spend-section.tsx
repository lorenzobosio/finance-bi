// safe-to-spend-section — STUB (filled by 09-04, SAFE-01). Accepts the page-resolved
// { demoFilter, asOf } props so the /cashflow page mounts it unchanged next wave, and returns null
// until the safe-to-spend engine + KPI land. No demo-bearing read yet (nothing to thread).

export async function SafeToSpendSection(_props: {
  demoFilter: boolean;
  asOf: Date;
}): Promise<null> {
  return null;
}
